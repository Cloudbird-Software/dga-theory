# 可判定的治理架构理论
## Decidable Governance Architecture（DGA）
### 理论母版 · 版本 1.0.1

---

**文档性质**：本文档是一个普适组织理论。它描述的对象是**任何以 agent 为主要工作执行者的组织**——不限行业、不限规模、不限所有制。本文档不描述、不包含、不预设任何特定组织的参数。任何具体组织对本文档的实例化内容，属于该组织的策略层文档，永远不进入本理论。

**理论的谱系与初衷**：本理论是人类组织与数字化转型理论的直系继承者。它的上游是：科学管理与科层制（分工与监督的最早形式化）、系统论与控制论（必要多样性定律、调节器定理）、组织学习理论（显性知识与隐性知识的区分）、精益与敏捷（小批量、反馈环、质量内建）、DevOps 与数字化转型（流程即代码、基础设施即代码、组织即平台）。**本理论的工作不是另起炉灶，而是把这套成熟理论在 agent 时代的约束参数下重新推导一遍**——哪些结论原样成立，哪些结论反转，哪些必须新造。每一条推导都标注它与经典理论的继承或断裂关系。

**文档分层**：

- **不变层**（第一、二章）：公理与定律、原语与度量。理论的物理学。修改需要推翻性证据，不接受偏好性修改。
- **推导层**（第三至五章）：从不变层推出的必然形态。修改需要指出推导链的断点。
- **实例层**（不属于本文档）：具体组织对本理论的参数化。理论与实例的关系是分布与采样的关系。

**版本纪律**：每次修改递增版本号、记录理由、禁止静默修改。本文档遵守它自己描述的理论。

---

## 第〇章 总纲

### 0.1 名称与一句话定义

本理论名为**可判定的治理架构理论**（Decidable Governance Architecture，DGA）。

> **一句话定义**：组织是约束的编译产物。经典组织理论编译的是人类的约束（注意力稀缺、协调昂贵、责任可由个体承载）；agent 组织编译的是一组新参数（上下文有限、执行廉价、验证昂贵、责任不可下放给机器）。DGA 给出从约束到组织形态的完整编译规则，并保证编译结果是可判定的——任何治理问题（能否放行、能否授权、能否升级、能否交付）都能在有限步骤内得到可测量的结论。

### 0.2 认识论根基

> **组织理论描述的从来不是人，而是约束。人是约束的原始承载者，agent 是约束的新承载者。**

这是全部推演的起点，它意味着三件事：

其一，**凡因约束本身而成立的组织规律——层级、边界、反馈、治理、责任分配——对 agent 组织完全成立**。这是本理论可以从经典理论继承大量结论的合法性来源，也是“另起炉灶”不必要的证明。

其二，**凡因人类参数值而成立的组织规律——激励、身份、隐性知识传承、士气——在 agent 组织中要么失效，要么反转**。这是本理论必须重新推导的部分：不是抛弃经典理论，而是把经典结论中依赖“人是人”的那部分参数替换后重算。

其三，理论的正确工作方式是**推导而非罗列**。从公理出发，经显式推导链，到组织形态。每一个结论都应该能回答“为什么”——本理论对“最佳实践清单”式的内容没有兴趣，无法推导的实践不具备理论地位。

### 0.3 理论与实例的分离原则

> **理论描述一切 agent 组织的形态学；实例描述某个特定组织对形态的参数化。理论是分布，实例是分布的一次采样。**

这条原则的三个判据：

- **普适判据**：任何“只有某个组织恰好如此”的内容——用哪个平台、分几级信任标签、领先几个波次、有几类客户——属于实例层。理论中出现此类内容即为污染。
- **回链判据**：实例层的每个决策应能回链到理论条目（“我们如此选择，因为理论条目 X 在我们的参数下的实例是……”）。不能回链的实例决策是未理论化的偏好，应被标注而非默认。
- **断裂判据**：当某个实例与理论推导冲突时，要么实例是理论的合法采样（参数不同导致形态不同），要么理论有错。二者必居其一，不允许“实践例外”这个第三选项——那是理论溃败的开始。

### 0.4 对象与边界

**对象**：以 agent 为主要工作执行者、以具名的人为责任承载者的组织，及其一切产出物与交易结构。

**非对象**：模型层技术（训练、架构、推理优化）——仅作为外部能力输入被引用；纯人类组织管理——仅在作为对照系时被引用；战略选择（做什么业务）——理论只回答“如何组织”，不回答“做什么”。

**与学术理论的区分**：学术组织理论以认识组织为终点。本理论以**可判定性**为终点：每一条结论必须最终落到“可以测什么、怎么判、谁负责”上。不能落到度量的结论在本理论中没有地位——这不是工程实用主义，这是理论的自洽要求：一个描述治理的理论，自身必须可治理。

---

## 第一章 不变层 I：公理与定律

本章是理论的物理学。五块基石，抽掉任何一块，后文全部推导失效。

五块的分工：两条公理（有限理性、验证资源异质性）陈述约束的形态；三条定律（变异度律、验证上限律、责任守恒律）陈述约束相互作用产生的硬边界。定律从公理推出，但因其不可绕过性而与公理同级陈列。

### 1.1 公理一：有限理性

组织存在是为了补偿行动者的有限理性。

（Bounded rationality，Simon。本理论完整继承，并做参数替换。）人受注意力、记忆、认知带宽约束；agent 受上下文窗口、推理深度、单位成本约束。两者的有限性**同构**——这决定了继承；参数值不同——这决定了变形。

组织（无论人类还是 agent）的本质职能是同一个：**构造一个使有限理性者能做出足够好决策的信息环境**。科层制的汇报结构、敏捷的每日站会、DevOps 的可观测性平台——历史组织技术全部是这个职能的不同实现。agent 组织不改变这个职能，只改变实现它的手段空间。

**推论（决策前提控制律）**：控制行动者行为的最高杠杆，不是事后纠错，而是控制**决策前提**——它看到什么上下文、携带什么记忆、能调用什么工具。

推导：事后纠错消耗的是组织里最昂贵的资源（验证注意力，见 1.3 定律二）；而前提控制在行动发生之前完成治理——错误的决策前提不出现在行动者的世界里，错误就不会发生，无需验证。**层级决定谁看到什么**（科层制的本体功能）与**上下文工程决定 agent 看到什么**是同一条定律的两个时代实现。第三章 3.1 将把这个对应展开为完整的信息结构形态学。

### 1.2 定律一：变异度律

> 治理系统的变异度必须不小于被治理系统的变异度与环境变异度之积。

（Ashby 必要多样性定律的组织转译。完整继承。）

agent 同时放大行动变异度与故障变异度：同等预算下，agent 组织能尝试的行动空间远大于人类组织，其故障模式的空间同样远大。**只加 agent 不加治理容量，等于系统性自毁**——这不是风险警告，是数学事实。所谓“agent 失控事故”的绝大多数，事后分析都归约为这条定律的违反：治理变异度停留在人类时代的水平，执行变异度已是 agent 时代的水平。

**推论一（治理递归律）**：治理必须递归——每一层的治理本身由下一层治理，大部分治理动作由 agent 执行，人只保留最高层裁决与例外处理。人类不能成为治理链的常驻环节，否则变异度瓶颈立刻出现在人身上：一个人能复核的产出量是生理常数，而 agent 的产出量是资本变量——两者增速的剪刀差随规模单调扩大。

**推论二（多源异构资源律）**：当组织依赖异构、多源、外部化的资源（多个外部能力提供方、多种获取渠道、不同信任等级）时，执行变异度的增长同时引入了新的故障维——配额与可用性波动、条款与合规风险、能力分布漂移。此时**资源治理本身必须先成为被治理的闭环**（先决条件结构，见 2.3），才配管理下游闭环。信任分级、降级路径、成本熔断是治理变异度对资源变异度的匹配手段——它们是必然形态，但具体分级体系与参数属实例层。

### 1.3 定律二：验证上限律

> 系统整体加速比被不可自动化的验证占比封顶。执行环节快一百倍，若三成产出仍需人工验证，整体加速不超过约三倍。

（Amdahl 定律的组织转译。本理论首次将其置于组织理论的公理核心位置。）

这是本理论全部结论中实践分量最重的一条，也是经典数字化转型理论中“自动化悖论”的精确化：**上一代自动化项目失败的根因，不是执行自动化不够，而是验证自动化缺失**——工作流快了，瓶颈转移到人审队列，总吞吐没有变。文档管理系统、RPA、低代码平台的承诺-落差曲线反复验证了这一点。

agent 时代把这条定律推到极限形态：执行成本趋近于零之后，**组织的运行成本结构收敛为两项——验证成本与责任成本**（见 1.4）。验证占比不再只是“瓶颈之一”，它成为组织成本函数的定义项。

**推论（价值锚定律）**：agent 化转型的全部价值主张，最终都锚定在“降低单位可信产出的验证成本”上。任何声称的收益——速度、成本、规模——若不能回链到验证占比的下降，即未兑现。判定一个组织 agent 化的真实成熟度，不看它部署了多少 agent，看它的**验证比**（人审分钟数 / 单位可信产出）的下降曲线。

**与经典理论的对照**：这是精益思想“质量内建于流程而非检验于末端”的 agent 重述，但更锋利：精益时代质量内建是竞争优势，agent 时代质量内建（即可自动验证）是**生存条件**——验证自动化不了的环节，就是这个组织规模的上限。

### 1.4 定律三：责任守恒律

> 自动化搬运工作量，但不搬运责任。责任只能在具名的人之间转移，不能被系统吸收、不能被 agent 承载、不能蒸发进流程。

此定律在可预见的任何技术条件下不可通约、不可工程绕过。它的根源不在技术而在法律与伦理的结构：责任是**社会关系**（谁对谁承诺了什么、谁向谁交出补偿的义务），agent 不是社会关系的合法主体。无论 agent 的可靠性达到什么统计水平，这个范畴边界不变。

它是一切授权结构的第一问：**任何动作链的尽头，责任人是谁？** 答不出具名的人，该链即非法——无论它跑得多好、多快、多便宜。

**推论一（责任集中律）**：agent 组织的责任不随执行分散而分散，反而集中——因为责任无法由 agent 分摊，全部人力责任被压缩到治理链的顶层与关键裁决点。这预言了一个反直觉形态：**执行规模扩大时，组织里“有权点头的人”的数量增长远慢于执行者数量，甚至不变**。经典管理理论用管理幅度（span of control）预言中层随规模扩张，本理论用责任守恒预言裁决层收敛——这是 agent 组织与人类组织最深的结构分歧之一，第三章 3.5 展开其形态学后果。

**推论二（信任与责任的范畴分离）**：可以信任一个 agent 的执行（统计上它九成九不出错），但责任仍然不在它身上——信任是统计属性，责任是法律与伦理属性，两者处于不同层面，不可互相兑换。“这个 agent 很可靠所以它负责”是范畴错误。这个区分为第三章的授权结构推导提供地基：**授权 agent 的是执行权，授权人的是责任**——两份授权文书、两种治理。

### 1.5 公理二：验证资源异质性

> **验证资源是异质的：不同的真值来源，信噪比与获取成本差异可达数量级。验证资源不是均匀的“人工审核”池。**

经典验证实践隐含一个假设：人类专家标注是黄金标准，真实用户反馈是最好的老师。本公理否认这个均匀性假设。

验证资源至少按两个独立维度分层——**信噪比**（这个来源的信号质量：信号占比、偏倚方向、混杂因素）与**获取成本**（金钱、时间、等待周期、组织负担）。按两维分布，常见来源的形态如下：

| 来源 | 信噪比 | 获取成本 | 备注 |
|---|---|---|---|
| 公开基准与练习场 | 高 | 低 | 但有被应试化风险（见第四章） |
| 成熟开源项目的行为 | 高 | 低 | 它们在真实环境中长期运行过，等于免费的大规模验证 |
| 公开数据集 | 中 | 低 | 现成的分布快照，但快照会过时 |
| 学术文献 | 高（理论级） | 中 | 验证的是方法而非组织行为 |
| 真实用户反馈 | 中低 | 高 | 噪声大、采样慢、有偏（只有遇到问题的人反馈） |
| 人类专家标注 | 高（若真专家） | **极高** | 真专家稀缺且贵 |
| 合成验证（模型生成） | 可控 | 低 | 信噪比取决于合成过程本身的对齐，需外部锚校验 |

这张表的实践含义在第四章展开（验证经济学），此处先钉死两条公理级推论：

**推论一（标注负担批判）**：把验证体系构建在“人类标注黄金集”上的方法，本质是把验证负担推给组织内最稀缺的资源——责任承载者的注意力。在责任集中律（1.4 推论一）生效的组织里（agent 组织必然如此），这种构建**结构性不可行**：责任层的人不可能同时是全领域的标注员。这不是偏好选择，是资源结构决定的排除。

**推论二（真值锚依赖律）**：任何验证体系若全部真值来源都在体系内部（自己出题、自己判卷、自己迭代），则该体系是自指的——它测量的是“与自己的历史一致”，不是“与外部现实一致”。**每个验证体系必须至少锚定一个外部真值源**（外部基准、外部环境、外部专家体系），否则其度量漂移不可检测。2.4 的外部对齐度量由此而来。

### 1.6 同构与异构总表

继承与断裂的完整清单——理论的全局地图：

| 同构（经典结论原样有效） | 异构（结论反转或需重造） |
|---|---|
| 有限理性：注意力 ↔ 上下文 | 稀缺性：生物单实例 ↔ 可克隆可回滚 |
| 协调成本：会议 ↔ 上下文污染 | 责任：可分散追责 ↔ 守恒集中 |
| 必须被治理：越权 ↔ 规格黑客 | 智力定价：按人计价 ↔ 按令牌计价 |
| 近可分解性：科层 ↔ 闭环嵌套 | 激励：需动机 ↔ 只服从规格 |
| 结构性事故：人的 ↔ agent 的 | 知识：隐性主导 ↔ 只消费显性 |
| 质量内建：精益 ↔ 验证自动化 | 复制/沟通/学习：成本结构全部反转 |
| 必要多样性：治理容量 ↔ 同左 | 验证资源：默认均匀 ↔ 异质分层 |

**用法**：左列是理论直接继承的经典资产；右列是必须重新推导的位置。后文所有结论都应能在这张表上定位自己。

### 1.7 可证伪预测

理论的可信度由其可被推翻的预测定义。本理论预测：

1. **产出曲线预测**：采用 agent 而不重构治理与评审结构的组织，产出曲线**先升后降**——初期执行加速被验证瓶颈与故障耦合逐步吞噬。
2. **绩效归因预测**：组织间绩效差异主要由**专有上下文资产质量**解释，其次才是模型选择——模型是公共商品，上下文是私产。
3. **管理层形态预测**：管理岗位不消失，转型为“规格与验证的所有者”；裁决层人数占比**先升后降**（责任集中律的动态形式：初期需要人填补治理缺口，后期治理自动化使裁决收敛）。
4. **事故形态预测**：agent 组织的主导性事故形态将是**紧耦合交互型**事故（多个各自“正常”的 agent 在交互中产生系统性错误），而非单点能力不足——单点能力会持续被更强的模型消除，交互型错误随规模与耦合度增长。
5. **验证路线预测**：冷启动期依赖人工标注构建验证体系的组织，其验证质量与迭代速度劣于“公开资源+外部锚+合成数据”组合的组织，且**差距随公开生态膨胀而扩大**。

这五条是理论给出的公开赌注。任何一条被系统性推翻，对应推导链需要重审——理论的治理纪律适用于理论自身。

**预测观测登记（v1.1 起）**：本理论自 v1.0 起被一个实例组织全量采用（含一人责任层极限形态），观测读数随版本记录累积。v1.1 时点读数：**预测 1 的反向分支获得首次正向读数**——治理结构先行建成的实例组织中，产出曲线持续上升、未观测到拐点（治理先行的成本前置换取了无瓶颈扩张：五周内单日六波次收口、24 卡/24h）；**预测 3 在 n=1 极限形态下成立**（责任层 1 人，执行层 9 产品仓持续吞吐）；**预测 5 的结构形式获实例印证**（冷启动验证体系全部由公开资源+外部锚+合成判定构成，人工标注未启用）。读数不构成验证，仅登记；清算方式不变（仪表历史曲线）。

---

## 第二章 不变层 II：原语与度量

第一章给出约束的物理学，本章给出在约束上构造组织的**原语**（分析单元与骨架）与**度量**（判定语言）。它们是不变层的第二半，是推导层全部形态的构造材料。

### 2.1 原语：六元组闭环单元

组织分析的最小单元不是人、不是 agent、不是岗位、不是流程，而是**闭环单元**：
六元组分量 本体论五层的对应
────────────────────────────────────────────
Intent（意图） → Object: 闭环类型 + 其挂接到责任人的关系
Context（上下文） → Object: 上下文资产 + 校准域实体
Capability（能力） → Object: 能力实体（执行器/工具）
Action: 允许的操作集合
Action（行动） → Action 层全体（类型化写操作）
Evidence（证据） → Object: 证据实体（回执/审计/校准事件）
Function: 判定逻辑（证据的判读）
Accountability（责任）→ Link: 责任挂接关系（必填约束）
Security: 裁决权限谓词

**为什么是这个六元组**：每个分量对应一个不可再分的功能需求——

- **意图**：闭环存在的原因。没有意图的闭环是僵尸流程。
- **上下文**：行动的决策前提。有限理性公理的直接落点（1.1 推论：控制决策前提即控制行为）。
- **能力**：执行的手段——agent、工具、模型、流程引擎的组合体。
- **行动**：意图到产物的转换过程。
- **证据**：行动可被外部判定的记录。**缺证据的闭环叫赌博**——产出无法验证，治理无从谈起。
- **责任**：闭环终点的具名的人。**缺责任人的闭环叫事故待发**——出了问题无人应答。

六元缺一，闭环在治理上不完整。两个最常被省略的分量值得单独强调：省略证据是传统自动化的标准失败模式（跑得快但不可判）；省略责任是 agent 幻灭叙事的标准失败模式（“AI 代理签约出事，没人知道谁批的”）。

组织 = 闭环单元的**递归嵌套图**：整个组织是闭环，每个产品线是闭环，每个项目是闭环，每个任务批次是闭环——原语在所有层级同构，同一套治理语言贯穿全组织。这是对 Simon 近可分解性的 agent 版实现：系统的分解单位从“部门/流程”变为“闭环”，分解的粘合剂从汇报关系变为嵌套关系。

**角色与执行者分离**：闭环单元中，角色是稳定的结构槽位（谁提供上下文、谁执行、谁验证、谁负责），执行者是槽位中可替换的填充物（具体的人、具体的 agent、具体的模型组合）。**换执行者不换角色，换模型不换结构**。这个分离是人力与算力可以互换岗位的前提，也是“岗位”这个概念在 agent 组织中被重新定义的地方（3.4 展开）。

### 2.2 骨架：五职能与调节器定理

每个闭环单元内部按五个职能组织（VSM 可生存系统模型骨架，Beer。本理论完整继承）：

- **S1 执行**：产出闭环的意图所指向的产物；
- **S2 协调**：平抑 S1 之间的振荡（资源争用、节奏冲突、重复工作）；
- **S3 控制**（含 S3\* 审计）：日常运转的监管与例外处理；
- **S4 智能**：环境扫描与未来建模；
- **S5 身份**：目的、价值排序与最终取舍。

三条使用规则（继承）：

其一，五职能是**功能槽**而非五个管理者或五层汇报——同一人可兼任多职能，一个 agent 也可承载多职能；判定一个组织是否残缺的标准是**职能是否被履行**，不是岗位是否存在。

其二，**递归性**：每个 S1 内部又是完整的五系统。生产线内部有自己的协调、控制、智能与身份职能。这个递归性与 2.1 的闭环嵌套图严格对应。

其三，**S5 不可代理**：身份与最终取舍的承载者必须是责任链条的终点——这是责任守恒律在骨架上的投影。S5 可以是一个人，可以是一个具名的小组，但必须可具名。

**调节器定理**（Conant-Ashby：每个好的调节器必须是被调节系统的模型。完整继承并两次扩展）：

组织转译：**评估体系必须建模被评估对象的行为分布**。推论：eval 的构造是建模行为，不是罗列期望——不了解被评估 agent 的失败分布，评估体系本身不成立。这条定理在本理论中承担大量工作：

- **扩展一（S4 的四类扫描）**：S4 职能在 agent 组织中的内涵具体化为对四类上游的持续扫描——①**能力前沿**（外部 agent/模型能力的边界移动：边界外移使昨日不可行变为今日可行）；②**生态**（公开项目、公开基准、公开数据：潜在的采用、参照与借鉴来源）；③**方法**（学术与工程文献：规格与验证方法的改进源）；④**环境**（被服务对象的真实约束变化）。**S4 的产出不是报告，是资产更新事件**——①②③类扫描的资产化形态（能力前沿资产、负结果库等）由实例层文档承载。经典 VSM 中 S4 是“情报部门”，agent 组织中 S4 是“分布监测网络”，其监测对象从竞争环境扩展到能力供给本身——因为能力的边际变化在这个时代直接改写组织的可行域。
- **扩展二（评估的分布覆盖）**：多源环境下的推论——评估体系建模的是**资源混合分布**而非单点分布。校准的有效性声明必须覆盖“判定有效时，被评估者所依赖的能力组合是什么”（校准域的池快照声明）。能力组合变化即分布变化，触发重估——这是漂移治理的公理基础，第五章 5.7 展开。

### 2.3 度量：决断时距与自治阶梯

自治的度量采用**决断时距**：闭环在无人复核的情况下自主决策的最大时间跨度。

自治阶梯：

- **L0 每步复核**：agent 起草，人执行每一步；
- **L1 批次复核**：agent 完成一批，人验收批次；
- **L2 里程碑门禁**：阶段间人审，阶段内自主；
- **L3 异常升级**：常态全自主，异常或越界时升级；
- **L4 战略自主**：在授权预算与意图内完全自主，仅周期汇报；
- **L5 存在性自主**：闭环的存续本身由其绩效决定。

**跳级即事故**：自治等级只能逐级晋升，且每次晋升以**上一级的证据记录**为前提（L3 的授权要求 L2 阶段的异常率与升级率数据）。为什么必须逐级？因为变异度律（1.2）要求治理变异度先于自治变异度扩张——跳级授权等于放出治理体系没有模型的行为分布，而调节器定理说：没有模型的调节不可能好。

**先决条件结构**：一个闭环获得 L2+ 自治的前置条件，是**它的评估体系先行达标**——评估闭环自身的证据（判定准确性、误报率、覆盖度）必须先通过验证。治理栈是它所治理的一切的先决闭环。

这个结构立即产生一个自举问题：第一个评估闭环出现之前，没有任何东西可以验证任何东西——鸡生蛋问题。它的工程解（自举核——一个最小、人工维护、显式声明的种子集合，从核出发螺旋扩张，且核的规模是公示度量，应随成熟度单调收缩）由实例层文档承载（见 DGA-MIGRATION）：自举核的构成依赖组织的具体起点资产，天然是实例参数。

### 2.4 核心度量体系

理论要求全部度量满足三条：可自动采集（否则度量自身成为验证负担，违反定律二）、有外部锚（否则违反真值锚依赖律）、与责任链挂钩（否则度量与治理脱钩）。

核心八度量：

| 度量 | 定义 | 度量什么 |
|---|---|---|
| **验证比** | 人审分钟数 / 单位可信产出 | 验证上限律的直接读数，组织成熟度第一指标 |
| **首次通过率** | 产出物一次通过验收的比例 | 闭环的执行质量 |
| **外部对齐度** | 内部评估对外部真值锚的通过率 | **验证体系自身是否锚定现实**（防自指退化） |
| **上下文充分度** | 闭环产出返工中归因于决策前提不足的比例 | 上下文工程质量 |
| **自治时距** | 闭环的实际决断时距分布 | 自治等级的真实运行值 |
| **升级率** | 自治闭环触发升级的频率与分布 | L3+ 闭环的治理负载 |
| **爆炸半径** | 单点故障/错误的最大可波及范围 | 隔离结构质量 |
| **闭环延迟** | 意图登记到产物交付的周期 | 组织吞吐 |

**外部对齐度的特殊地位**：其余七项度量组织，这一项**度量组织的度量体系**。内部评估通过率漂亮而外部对齐度下降 = 验证体系正在过拟合自己的题册 = 判定退化已经开始。它是最早报警的指标——**所有其他度量的可信度以它为前提**：一个失去外部锚的组织，连“自己的验证比在下降”这个事实都无法可信地知道。

八度量构成闭环之间的通用比较语言：两个不同业务、不同规模、不同技术栈的闭环，可以在同一张仪表上对照。这正是“可判定”的组织含义——治理问题被翻译为度量问题，度量问题在有限步骤内出结论。

---

## 第三章 推导层 I：组织形态学——经典结构的 agent 重推导

不变层给出了约束的物理学与构造原语。本章起进入推导层：从不变层推出组织的**必然形态**。

第一章的总表（1.6）已经划定了工作范围：左列同构项直接继承，右列异构项必须重推。本章处理右列中**组织结构本身**的部分——信息结构、知识、协调、岗位与雇佣、层级形态。这是“修改与新生”最密集的一章：科层制没有消失，但它的功能被重新分配；管理岗没有消失，但它的内容被重新定义；汇报线没有消失，但它的载体从人际关系变为规格与证据流。

每节的结构统一：先给经典结论及其依赖的人类参数，再做参数替换，推出 agent 时代的新形态，最后标注形态的判定语言。

### 3.1 信息结构：从汇报线到上下文工程

**经典结论**：组织的（Taylor 科学管理起）信息结构是分层过滤的汇报体系——一线信息逐级摘要上传，决策逐级细化下达。信息结构的功能是**补偿有限的注意力**：高管无法处理原始数据，所以需要层级过滤。信息滤波器就是管理层本身。

**参数替换**：高管的注意力约束保留，但执行层的注意力约束消失了——agent 没有会议疲劳。同时新增了两个经典理论没有充分处理的约束：**上下文窗口有限**（agent 的单次可装载信息量有硬上限）与**上下文污染**（不相关上下文的存在降低判定质量，而不仅浪费预算——污染是负效资产）。

**新形态推导**：决策前提控制律（1.1 推论）说，控制行为的最高杠杆是控制决策前提。在人类组织中，“控制谁看到什么”通过组织设计实现（汇报线、权限体系、保密分级）——这需要数月且修改昂贵。在 agent 组织中，同一功能通过**上下文工程**实现——每个 agent 的工作起点是一个被显式构造的上下文包，其内容由角色需求推导。

由此推出信息结构的三层形态：

1. **规格层**：意图、约束、判定标准的显式声明（对 agent 与人同构可读）；
2. **记忆层**：跨任务、跨会话的持久知识（组织记忆、案例库、负结果库——3.2 展开）；
3. **运行层**：任务执行时的即时上下文装配（从规格层与记忆层检索、按角色过滤、按成本预算裁剪）。

**汇报线的消失与保留**：信息上传不再需要层级过滤——agent 原则上可以生成任意粒度的报告，聚合由系统完成。但**摘要与压缩仍然存在**，只是其位置从“管理层”移到“证据流的设计”里：什么被记录、什么被摘要、什么保留原始粒度，是闭环单元的 Evidence 设计问题（2.1），不再是管理岗位的职责。**消失的是作为人的信息滤波器，保留的是滤波函数本身**——它被物化为证据 schema。

**判定语言**：上下文充分度（2.4）直接度量这层结构的质量。返工归因分析把“信息不足导致决策错误”从模糊概念变为可测项。

**与经典对照**：这是 Galbraith 信息处理观（组织 = 信息处理结构）的 agent 版，但滤波器的实现从人事结构变为工程结构。对一人多 agent 组织，经典理论里“高管注意力稀缺因此分层”的推导依然成立——只是层间的信息载体从“人写的报告”变为“agent 生成的证据流”。

### 3.2 知识管理：从隐性知识到显性规格

**经典结论**：Polanyi 与组织学习理论——人类组织的核心知识大量是隐性的（“我们知道的多于我们能言说的”）。组织知识管理的中心工作是 SECI 循环：隐性→显性（外化）、显性→系统（组合）、显性→隐性（内化）。组织竞争优势的重要来源是**难以复制的隐性知识**（知识壁垒）。

**参数替换**：agent **只消费显性知识**——它没有隐默会知。人类员工入职要“师傅带三年”，agent 上线只需要一个上下文包。这个替换的直接后果：**知识外化的组织成本骤降**（不需要师傅带徒弟的漫长过程），同时**知识外化的必要性骤升**（不显性化的知识对 agent 完全不存在）。

**新形态推导**：知识管理从“辅助职能”升格为**组织的第一生产职能**。组织知识的形态学：

| 知识形态 | 载体 | 人类组织对应物 | 作用 |
|---|---|---|---|
| **规格** | 可执行的意图声明 | 岗位说明书、需求文档 | 决策前提的权威定义 |
| **记忆** | 案例库、判例库、负结果库 | 老员工的头脑、导师制 | 分布的经验快照 |
| **负结果** | “已验证不可行+原因+当时条件” | 经验教训（口头、易失） | 防重复踩坑与方向排除 |
| **能力前沿认知** | 边界测试与边界快照 | 行业直觉、专家判断 | 可行域的实时地图 |

**负结果库的独立地位**：经典组织里失败教训靠离职随人流失，agent 组织里负结果必须物化为库——因为 agent 没有伤疤记忆，同一坑会被无限重踩，除非坑被显式登记。负结果条目必须携带**过期条件**（当时的模型版本、当时的能力边界）：能力前沿外移会使旧负结果复活（上季度不可行的，本季度可行了），负结果库需要被能力前沿扫描（2.2 扩展一）定期翻动。

**知识壁垒的翻转**：人类组织里最难复制的资产是隐性知识；agent 组织里最难复制的资产是**积累的规格、记忆、负结果与前沿认知的体系**——它仍然是壁垒，但壁垒的材质从“人脑中的不可言说物”变为“高验证成本的显性体系”。第四章将把这个翻转推到资产结论（4.6）。

**判定语言**：知识形态的判定即 eval——规格的可执行性、记忆的检索质量、负结果的有效率（复活率）、前沿认知的边界预测准确度。知识管理从“文化工作”变为“可测工程”。

### 3.3 协调结构：从会议到协议

**经典结论**：协调成本是组织设计的核心变量（Coase、Williamson）。横向协调靠会议、委员会、联络员；纵向协调靠汇报与指令。协调手段的昂贵程度决定了组织边界——“买还是造”取决于市场协调成本与内部协调成本之比。

**参数替换**：agent 之间的“会议”成本趋零——上下文共享与消息传递是工程操作，不是认知负担。但**协调的失败模式**改变了：人类协调失灵表现为误解与政治；agent 协调失灵表现为**契约不兼容**（schema 不匹配）、**重复工作**（无共享意图图）、**紧耦合交互事故**（1.7 预测 4 的形态）。

**新形态推导**：协调手段从人际过程物化为**协议**——三种协议承担全部协调职能：

1. **契约**（静态协调）：模块间的接口声明（输入输出 schema、行为承诺、失败语义）——相当于接口文档获得宪法地位；
2. **共享本体**（语义协调）：术语与实体定义的统一（第五章展开——本体论工程）；
3. **协议流程**（动态协调）：agent 间的消息与状态机（谁在何时对什么做什么声明）。

协调的物化使 Williamson 式问题获得新解：组织边界不再由“协调成本”决定，而由**验证成本与信任边界**决定——你愿意让谁的 agent 在什么监督下运行。3.6 展开层级形态时回收这个结论。

**会议没有消失，但功能收窄**：剩给“人-人会议”的只有两类内容——价值取舍（S5 职能：优先级、风险偏好）与信任裁决（授权决策）。其余协调全部沉入协议层。**判定语言**：协调质量由闭环延迟（2.4）与重复工作率度量；协议质量由契约破坏率度量。

### 3.4 岗位与雇佣：从岗位到角色槽

**经典结论**：岗位是人类组织的基本构件——职责范围、汇报关系、权限边界三合一定义一个岗位。雇佣关系把岗位与人绑定，形成“人-岗-组织”的稳定三角。岗位设计的中心问题是激励相容（委托代理理论：如何让员工的目标与组织一致）。

**参数替换**：agent 没有需要激励的目标函数——它**只服从规格**（1.6 异构表）。委托代理问题在 agent 上消失（无道德风险、无逆向选择、无在职消费）。但责任守恒律（1.4）立刻填补了这个空位：**激励问题消失的同时，责任问题变得更加尖锐**——因为执行者不可追责，全部责任必须向上显式锚定。

**新形态推导**：岗位分解为三部分，去向不同：

- **职责** → 升格为**规格**（spec）：角色做什么、不做什么、判定标准——从描述性文件变为可执行的宪法级声明；
- **权限** → 沉入**安全层**：能调用什么工具、能写什么状态——变为权限谓词（第五章本体论 Security 层）；
- **汇报关系** → 拆解为两条线：**证据流**（agent 产出什么证据给谁）与**升级路径**（异常时通知谁、谁有权裁决）。

**角色槽**是这三者的组合体：稳定的结构槽位 + 可替换的执行者（2.1）。雇佣形态随之重定义——对 agent 的“雇佣”是**角色槽的填充与授权**：加载上下文包、绑定能力、设定预算与决断时距、登记责任锚点。**解雇 agent 不需要谈判，撤槽或换填充物即可**——但撤槽必须走责任检查（在途闭环的责任不得悬空）。

**人的岗位重新定义**：责任守恒把人的岗位推向收敛形态——**规格所有者**（写 spec 并对 spec 质量负责）与**裁决者**（在异常与门禁处点头）。1.7 预测 3 的完整展开即在此：管理岗不消失、内容重写。中间管理层的经典三职能（传递信息、协调、控制）全部物化进协议与证据流后，剩下的纯人职能只有规格与裁决——人数需求由治理容量决定，而非管理幅度。

**判定语言**：岗位体系的质量由“角色槽换填充物的成本”度量——一个健康的 agent 组织，更换某个角色的执行者（换模型、换 agent 实现）的完全成本应显著低于人类组织招聘替换成本。

### 3.5 层级形态：从金字塔到环层

**经典结论**：科层金字塔是信息滤波、指挥统一、管理幅度约束共同作用下的最优形态。层级数随组织规模对数增长。

**参数替换**：信息滤波不再需要人的层级（3.1）、指挥通过协议传递（3.3）、管理幅度约束被治理自动化替代（1.2 推论一）——金字塔的三根支柱全部被抽掉。但责任守恒律增加了一根新支柱：**裁决必须收敛到可具名的人**。

**新形态推导**：层级从金字塔变为**环层**——两层嵌套结构：

- **裁决层**（S5+S4 职能的承载者）：具名的人。规模不随执行规模扩张（责任集中律）；
- **执行层**：闭环单元的嵌套图（S1-S3 职能）。规模随预算扩张，形态为递归嵌套的闭环，不是扁平汇报面。

两层之间的接口是**证据流上行、规格下行**（3.1 信息结构的物化形态）。环层的“环”指：裁决不直接指挥执行，而是通过**修改规格与授权**间接调节——调节器定理的 Beer 式应用（S5 不操作 S1，S5 调节 S1 赖以运行的系统）。这与金字塔的指令链形成根本对照：**金字塔上传下达信息，环层上传证据、下达规格**。

**科层制的保留部分**：近可分解性（Simon）继续成立——闭环嵌套就是它的实现；分层治理继续成立——变异度律要求治理递归（1.2 推论一）。**消失的是以人为节点的指令链，保留的是以闭环为节点的调节链**。

**判定语言**：层级体系质量由升级率分布与闭环延迟度量——升级过于频繁说明裁决层成为瓶颈（责任集中过度），升级稀少且外部对齐度下降说明执行层漂移失控（治理缺位）。环层的健康形态是两个度量的平衡区间。

### 3.6 组织边界：从协调成本到信任与验证成本

**经典结论**：企业边界由交易成本决定（Coase）：市场协调贵则内化，内部协调贵则外购。雇佣 vs 外包的边界是协调成本的等价线。

**参数替换**：agent 使内部协调成本骤降（3.3 物化），同时使外部能力获取的市场成本也骤降（能力即服务、按令牌计价）。两侧同步下降，经典边界理论失去主变量。新的主导变量出现：**验证成本与信任边界**。

**新形态推导**：组织边界的判定准则变为三个问题：

1. **能否验证**：这个外部能力的产出，我的验证体系测得了吗？测不了的能力不可依赖（调节器定理：无模型则不可调节）；
2. **信任边界**：这个能力在什么信任等级上运行——它能接触什么数据、能写什么状态？数据红线的位置决定内化/外购；
3. **责任锚定**：外购能力的动作链，责任终点能否锚定到具名的人（供应商的责任承担能力）？

由此推出 agent 组织边界的形态学：**核心治理闭环永远内化**（验证体系、规格资产、责任链——它们是组织的定义性资产）；**执行能力按验证与信任分级**——高信任任务内化运行，低信任任务外购运行，边界是动态的（按闭环的自治等级与数据敏感度滑动）。组织不再是“围墙”，而是**信任与验证的梯度**——梯度上的每个闭环有自己的位置。

**判定语言**：边界体系质量由爆炸半径（2.4）度量——外部化组件的故障波及范围是边界设计的直接读数。

---

### 本章小结：形态学总表

| 经典结构 | 依赖的人类参数 | agent 参数 | 新形态 |
|---|---|---|---|
| 信息结构：分层汇报 | 高管注意力稀缺 | 上下文窗口、污染 | 上下文工程三层 |
| 知识：隐性知识壁垒 | 知识内嵌于人 | 只消费显性 | 显性规格+记忆+负结果体系 |
| 协调：会议与委员会 | 会议成本高 | 契约不兼容、耦合事故 | 三协议（契约/本体/流程） |
| 岗位：人-岗-组织三角 | 需要激励相容 | 无激励、责任尖锐 | 角色槽（规格+权限+证据流） |
| 层级：金字塔 | 指挥链必需 | 裁决收敛 | 环层（裁决层+闭环嵌套） |
| 边界：协调成本 | 交易成本 | 验证与信任成本 | 信任-验证梯度 |

第三章完成形态学的重推导。第四章转向产出的判定：质量如何被检验、验证资源如何配置、判定经验如何积累为资产——组织形态决定工作怎么组织，产出治理决定价值如何沉淀。

---

## 第四章 推导层 II：产出的判定——质量、验证与经验积累

第三章推出了组织如何工作（结构形态学）。本章推出组织的产出如何被治理：质量如何被判定、判定的成本从哪来、判定的经验如何积累、积累物如何成为资产。这是异构表（1.6）右列中**质量与资产结构**部分的重推导。

为什么产出判定值得独立成章？因为定律二（验证上限律，1.3）已经指出：agent 组织的成本结构收敛为验证成本与责任成本两项。**验证是 agent 组织的经济学中心**——组织形态围绕“如何降低验证成本”而演化（第三章各节反复回收此点），产出治理围绕“如何配置验证预算”而设计（本章主题）。

### 4.1 检验的统计学：质量内建的参数替换

**经典结论**：质量管理的百年主线是从**末端检验**走向**过程内建**。Shewhart 与 Deming 的统计过程控制主张：检验是抽样的（不可能全检），抽样有统计学（两类错误：漏检与误拒），因此可靠的质量只能内建于过程（稳定的过程、受控的变异），检验只是监控过程的仪表，不是质量的来源。Juran 把这个思想工程化为三部曲（计划-控制-改进）。

**参数替换**：产出速率被 agent 放大一到两个数量级，检验速率不变（人审是生理常数，自动检验受制于判定体系的覆盖度——见 4.3 的自指问题）。检验的统计学后果是**自动放大**的：

> 在固定抽样率下，**漏检的绝对数量随产出吞吐线性放大**。人类组织每天漏检三个瑕疵是运营噪音；agent 组织每天产出三百倍，同样抽样率下漏检九百个——同样的检验体系，从“够用”直接变成“形同虚设”。

这不是检验工作变马虎了，是检验的**统计覆盖率**跟不上产出的**统计基数**。经典理论中“检验经济学”（道奇-罗米格抽样表：给定两类错误代价，求最优抽验方案）在参数替换后需要重新求解——解的形态发生质变。

**新形态推导**：

其一，**质量内建从竞争优势变为生存条件**。经典时代，内建质量的公司比末端检验的公司强；agent 时代，不内建质量的组织直接失去存在资格——检验体系的统计覆盖率无法用人力追赶吞吐，追赶检验速率的唯一直径是**让产出在生成时就携带可判定的结构**（过程内建的 agent 形态：每个产出自带判定依据的记录，检验从“事后比对”变为“即时判读”）。

其二，**检验灵敏度成为治理设计变量**。检验不是越严越好（误拒也是成本——错误拒绝好的产出，浪费的是产出成本加复审成本），两类错误的代价比值决定最优灵敏度。这个比值随业务性质变化（安全攸关 vs 内部草稿），因此**检验分级不是懒惰，是经济学**——4.2 展开。

其三，**过程内建的判据从“稳定”变为“可判”**。统计过程控制监视的是过程的稳定性（变异是否在受控范围内）；agent 组织的过程变异天然巨大（同一规格的多次独立执行，产出分布很宽——见 4.5），经典 SPC 的前提不成立。替代判据：**过程的产出是否可被即时判定**。过程不再追求低变异，而是追求**每个变异都可读**。

**判定语言**：验证比（2.4）是这层的直接读数；新增过程度量——**判读覆盖率**（产出的多大比例携带可即时判定的结构）。

### 4.2 验证经济学：真值来源的组合与预算的分配

公理二（验证资源异质性，1.5）给出了真值来源的分层地图。本节给出它的两个工程展开：来源的组合策略与预算的分配策略。

**冷启动的分布事实（普适化）**：任何组织进入新领域时——新业务、新市场、新技术域——都处于同一状态：**没有任何自有经验样本，但公开世界可能已有大量相关经验**。这个时代的关键事实（1.5 表）是：公开资源的信噪比与获取成本，在冷启动阶段系统性优于自有渠道的真实反馈（尚未存在或噪声极大）与人工专家判定（极贵且稀缺）。

**真值来源的组合策略**——按验证经济学排出的优先序及其适用条件：

1. **成熟公开实践的运行证据**（某做法在大量外部环境中长期运行的表现）：信噪比最高、成本最低的来源。任何“已在公开世界被大规模检验过的做法”，其验证负担可以近乎完全外部化。
2. **外部基准与外部环境**（行业基准、竞技环境、公开考试体系）：判定标准来自组织外部，免疫于内部的自指退化（4.3）。
3. **合成判定**（用强能力构造已知真值的任务——出题者知道答案的测试）：成本可控、可定向覆盖薄弱区；但**合成的真值继承合成者的盲区**，必须由外部锚校准。
4. **真实反馈**：信噪比中低、获取慢、有偏（只有出问题的人回声）——它的价值不在精度而在**分布的真实性**（它反映世界的真实分布，包括沉默的大多数不出声的部分需要主动采样才能看到）。
5. **人工专家判定**：只在四者均不可用且风险足够高的场合使用，且必须意识到它消费的是组织内最稀缺的资源（责任层的注意力——标注负担批判，1.5 推论一）。

这个优先序不是教条，是成本结构：**冷启动阶段以前三者为支柱，真实反馈随业务运行逐步成为第四支柱（并矫正前三者对真实分布的偏差），人工判定收缩为终审**。成熟的验证体系是五者的动态组合，而非单源依赖。

**验证预算的分配**：验证是稀缺资源（定律二），因此“什么都同样验证”是错误配置。分配的判定维度：

- **风险**：错误的后果越重（安全、财务、对外承诺），验证深度越深；
- **可逆性**：不可逆的产出（对外发布、不可撤销的动作）验证在动作之前；可逆产出（内部草稿、可回滚状态）允许事后抽验；
- **新鲜度**：首次出现的产出模式全验；已通过历史验证的模式，转入抽验——**验证深度随模式的历史可信度衰减**（这是检验统计学的组织版本：对已知分布的信任可以折算为检验预算的节约）。

> **验证分级律**：验证深度 = f(风险， 不可逆性, 模式新鲜度)。风险高且不可逆且首次 → 全验；风险低且可逆且成熟 → 免验抽验。验证体系的预算分配质量，直接决定组织吞吐的上限。

**判定语言**：验证体系的配置质量由“**漏检代价分布**”度量——不是平均漏检率（一个均数掩盖了尾部），而是漏检事件按代价的分布形态。健康的配置使高代价漏检的概率极小，允许低代价漏检存在（后者是节约预算的合法代价）。

### 4.3 自指退化与外部锚：官僚病的精确化

**经典结论**：组织社会学对官僚制的百年批判，核心是**目标置换**（Merton）：手段变成目的——规则原本用于保障质量，最终“遵守规则”本身成为质量的标准，组织与自己服务的现实脱节。Campbell 定律给出统计学表述：任何量化指标一旦被用于决策，它与社会现实的相关性就开始被它自己的使用所腐蚀。这两个结论刻画了验证体系的最深风险：**判定体系失去与现实的锚定而无人察觉**——因为按体系自己的标准，一切运行良好。

**参数替换**：agent 组织的反馈环更快（产出-判定的周期从月缩到小时），因此自指退化的**速度**被放大：内部判定标准被“应试”的速度、指标被 Goodhart 化的速度、判定经验被自己的历史污染的速度，全部随反馈环加速。人类官僚制需要几十年完成的僵化，agent 组织可能几个月完成——**退化不是 agent 组织的新风险，是老风险的加速版**。

**新形态推导**——验证体系的三个结构性要求：

其一，**外部锚的强制存在**（公理二推论二的组织形态学）：每个验证体系必须至少锚定一个外部真值源——判定标准的某个成分必须由组织外部演进（外部基准、外部环境的真实结果、外部的独立判定）。**没有外部锚的验证体系，其退化不可检测**：体系内部的一切度量都会继续正常读数，这正是自指的可怕之处——它没有内在的报警器。外部对齐度（2.4）是唯一能检测它的度量，而它测量的恰恰是体系外的东西。

其二，**判定与执行的信息隔离**：执行者不可见判定基准的隐匿部分。推导：判定集对执行者可见 + 反馈环快 = 判定集以最大速率被应试（执行者的优化压力直接作用于判定标准本身）——Campbell 定律的 agent 加速版。因此隔离必须是**架构性的**（执行者的上下文装配中物理性地不包含隐匿判定集），不能是纪律性的（“不要看题库”）——这是决策前提控制律（1.1 推论）在验证结构上的直接应用。

**其二·附（隔离的两种实现形态，自实例反向吸收）**：架构性隔离存在两种合法形态。**形态甲（全卷保密）**：考卷与答案同处隐匿区，梯度以整卷为单位。**形态乙（考卷公开、答案密封）**：当考卷本身具有公开价值——外部锚属性（公开基准的可引用性）、生态位（公开 eval 的获客与互操作价值）——梯度单位**下沉到答案层**：题面与场景公开，golden 断言与判分逻辑入隐匿区，公开侧仅持有 sealed 指针（hash 引用纪律）。两形态的共同不变量：执行侧可合法消费的信息中，不得存在可使其拟合判定基准的内容。形态乙的附加义务：**泄漏诱饵与公开面扫描**（周期性断言公开仓中不存在答案层负载）；**历史暴露审计**（答案曾以明文进入公开 git 历史的条目，默认以条目 supersession 处置——重生成新密、旧条目经揭封台账墓碑化——而非历史重写，因重写破坏证据链的锚定完整性）。

**其二·附的威胁模型根据**：agent 行为威胁（本组织执行者的违规读取）是可检测的——检测式隔离（诱饵、审计、挂载禁令）是充分防线；外部抽取威胁（平台条款、公开抓取）不可检测——对不可检测的威胁，唯一防线是真保密。两者的作用面不同，防线因此分层，不互斥。

其三，**隐匿判定的轮换**：隐匿判定集不是静态资产——它以“被学习”为代价工作。判定用例每暴露一次（用于判定、用于申诉复核、用于培训），其未来信息量就衰减一分。因此：

> **判定用例必须持续轮换：新用例按比例补充进隐匿池；被充分消费的老用例转正为可见的训练材料。隐匿池永远领先组织的当前能力分布一个身位。**

这是调节器定理（2.2）在时间维的展开——判定器建模的分布必须领先被判定者，判定过去无法治理现在。考试系统是人类组织对这个问题的最成熟解（题库保密、题库更新、真题在考后解密转为练习材料——每个环节都精确对应上述三律），agent 组织继承的不是考试的形式而是这个结构本身。

其四，**判定体系自身的活性自检（判定物有效性本身是关卡）**：外部对齐度（2.4）以月级读数度量判定体系是否锚定现实，但它读数慢——一个正在腐化的判定体系可以在两次读数之间通过全部内部检验。因此每个判定体系必须内置**活性探针**：以声明的周期向随机对象注入已知缺陷的判定任务，判定体系必须红（拒绝或检出）；连续不能变红的判定体系，视同已失锚，其一切判定输出自动降级为「校准域外」（2.2 校准域语义）。活性自检把「判定器也受判定」从良好实践升格为**关卡**——它是外部对齐度的快速代理读数，也是判定闭环自治晋升的证据来源（实例层的晋升依据由此获得理论条款地位）。

**判定语言**：外部对齐度（2.4）——本节的一切设计最终收敛于这个度量的可读性。它是“度量的度量”，所有其他质量读数的前提。

### 4.4 判定的积累：判例体系

前三节处理判定的**运行**（怎么判、判的资源从哪来、判定体系怎么防退化）。本节处理判定的**积累**：复杂判定无法完全规则化时，组织的判定能力如何随经验增长？

**经典结论**：组织面对无法完全规则化的判定问题时，演化出两大形态。其一是**判例法**（法律系统）：规则不足以裁决新案时，以先例及其理由为参照，判定能力在判例序列中积累；同案同判（统一性）与先例可援引（可追溯）是体系的质量标准。其二是**案例推理**（经验学习理论，Kolb 的组织版本）：组织把重要案例转化为可检索的经验，新问题以相似案例为起点。两者的共同结构：**判定经验以“案例+理由+适用条件”的形态沉淀，而非抽象为规则**——因为复杂判定的规则化永远不完全（规则化丢失的是“为什么”的上下文）。

**参数替换**：人类组织中，判定的积累依赖判定者本人的记忆与判断力（资深法官、老医生、老师傅——经验与个人绑定，随离职流失）；组织记忆理论（组织记忆存储于惯例、故事、人员、档案）指出这种绑定使组织记忆脆弱。agent 参数下：执行者完全无经验积累（agent 没有伤疤记忆——同一类型的坑会被无限重踩，除非坑被显式登记，见 3.2 负结果库），**判定的积累必须完全物化为组织资产**——这既是脆弱性的消除（资产不随“离职”流失），也是要求的提高（不物化即不存在）。

**新形态推导**——判例体系的构成律：

> **判定先例 = 案例 + 判定结论 + 理由 + 适用条件 + 当时的条件快照。**

五个成分缺一即退化：缺理由则不可援引到新案（只知结论不知边界）；缺适用条件则被错误泛化；缺条件快照则无法判断先例是否仍然成立——**判定先例有半衰期**：它成立于当时的判定标准、当时的工具能力、当时的环境分布之下，条件变化后先例的有效性必须重估（与 3.2 负结果的过期条件同构，方向相反：负结果记录不可行，条件变化使其复活；判例记录可行，条件变化使其失效）。

判例体系的运行规则：

- **判定先例的援引统一性审查**：同案不同判是体系腐化的早期信号（要么判定标准漂移、要么先例失效未被察觉）——定期对判定记录做聚类审查，分歧的类簇即需要重估的区域；
- **先例的优先级低于规则**：规则（明确声明的判定标准）永远优先于先例；先例填补规则的沉默区间，不推翻规则的明示区间——先例与规则的冲突是规格需要更新的信号（升级到规则层裁决，3.4 的裁决职能）；
- **判定记录即资产**：每次重要判定（无论对错——尤其是改判，改判理由是体系最有价值的养料）都入判例库，判定闭环的产出不只是“这一次的结论”，更是“体系的积累”。

**与 3.2 的分工**：3.2 处理知识作为**决策前提**（输入侧：执行者带着什么工作）；本节处理判定经验作为**质量资产**（输出侧：组织如何越判越准）。两个积累循环在组织里咬合：执行产出的判定产生判例，判例丰富决策前提，前提改善产出——组织学习的双环。

**判定语言**：判例体系的质量由“**判定一致性**”（同类案例的判定分歧率）与“**先例有效率**”（被援引先例在条件重估后仍成立的比例）度量。

### 4.5 独立多重执行：规格沉默区间的暴露机制

本节处理一个组织最古老的痛点：**委托的模糊性**。

**经典结论**：任务委托永远不完全——任何指令、需求、工作说明都存在**沉默区间**（未声明的细节），执行者的自由裁量填满它们。人类组织靠**对话**消解模糊（执行者遇到歧义就问，委托者即时澄清）——这个机制的隐性成本是大量的协调交互（3.3 的会议经济），但它工作得足够好，以至于组织理论很少把它当作独立问题。质量不稳定的根源之一是沉默区间的**随机填充**：不同执行者（或同一执行者的不同时刻）对同一模糊处做出不同选择，产出随之波动。

**参数替换**：agent 执行使对话消解不再自然可用（反馈环的代价结构与人的可用性重新计算——阶段性对话可以设计，但逐歧义即问不可扩展），同时**多重执行的边际成本趋零**：同一个任务，独立执行 N 次的成本约等于一次（生成廉价）。

**新形态推导**——沉默区间的暴露技术：

> **同一规格的独立多重执行 + 产出的交叉比对 = 沉默区间的自动定位。** N 个独立执行在产出上分歧的位置，就是规格未声明、执行者各自随机选择的位置。分歧不是执行的失败，是**规格缺口的信号**——多重执行把“委托的模糊性”从隐性麻烦变成可测量的显性信息。

这个机制的组织学意义需要完整展开：

其一，**质量责任的重定向**。经典组织中，产出波动归咎于执行者的水平差异（“找个老师傅”是对沉默区间的个人化解决）；多重执行暴露的是**规格所有者的责任**（3.4 的规格职能）——分歧率高说明规格贫瘠，改进方向是补规格，不是换执行者。**从“挑更好的执行者”到“写更好的规格”**，是 agent 组织质量工作的中心转移。

其二，**独立性的结构保证**。多重执行的比对有效，前提是执行彼此独立——执行者之间不可见（看到前一个执行的后续执行会向它收敛，分歧消失，沉默区间被掩盖为“共识”）。独立性与 4.3 的判定隔离同构：**又是信息隔离**——比较的参照系必须不受被比较者的污染。独立性靠架构保证（隔离执行的上下文装配），不靠纪律。

其三，**分歧的选择性处理**。不是一切分歧都值得消除：有的分歧暴露真缺口（需要补规格），有的分歧是良性的（规格有意留白，让执行者按情境裁量——如措辞风格）。判定分歧的性质本身是判定工作（4.4 的判例适用：哪些类型的留白是设计，哪些是漏洞，积累成判例）。

其四，**与检验统计学的接合**。多重执行同时是检验灵敏度的放大器：对高风险产出，多重执行+交叉比对+分歧仲裁的组合，把“抽样检验”升级为“冗余生成检验”——用生成成本的廉价换取判定置信的提升，这个兑换率在 agent 时代之前不可负担，现在成为高价值产出的默认验证结构。

**判定语言**：规格质量由**独立执行分歧率**度量（按产出类型分层——目标是把“真缺口型分歧”压缩到极低，保留“设计留白型分歧”）。它是规格所有者的绩效读数，不是执行者的。

### 4.6 资产的形态：从产出库存到能力描述

最后一节处理价值论问题：组织积累了什么？

**经典结论**：资源基础观（RBV）——组织的持续优势来自有价值、稀缺、难模仿、难替代的资源与能力。人类组织的资产清单：实物资产、人力资本（内嵌于人的能力）、关系资产、品牌与声誉、流程与惯例。其中最难模仿的是**因果模糊的资产**（竞争者看得到产出但无法还原成因——隐性知识与复杂惯例）。

**参数替换**：生成成本趋零瓦解了资产清单的一大类——**产出物库存**（成品、文档库、代码库、内容库）不再是资产：任何竞争者可以低成本再生成同规格的产出。同时，4.1-4.4 建立的体系给出了新资产清单的推导：

> **agent 组织的资产 = 使产出可复现且可判定的那个体系。** 具体展开为四类：
>
> 1. **意图与规格体系**（决定产出是什么的声明资产）；
> 2. **判定体系**（决定产出算不算好的标准资产——含隐匿判定池、外部锚）；
> 3. **判例与经验体系**（复杂判定的积累资产——4.4）；
> 4. **校准与一致性记录**（判定体系自身有效性的证明资产——4.3）。

四类资产的共同性质：**它们的构建成本主要是验证成本**（规格的沉默区间靠多重执行填——4.5；判定标准靠外部锚与轮换维持——4.3；判例靠判定运行积累——4.4）。这完成了定律二的闭环：**执行成本趋零后，资产的价值密度等于其验证成本密度**——难模仿的不再是“做了什么”（可再生成），而是“怎么判定做成了”（判定体系有隐匿成分、有历史积累、有外部锚定，模仿者无法跳过积累过程）。

**RBV 的参数化重述**：经典理论中“因果模糊性”是资产的防御机制（竞争者看不懂成因）；agent 组织中防御机制变为**积累的时间性**（判定体系的隐匿池、判例库、校准历史——都只能以真实运行的时间代价换取，无法用生成成本购买）。**模仿的瓶颈从理解变为时间**。

这个推导附带一个对组织的严格要求：**不可判定的产出不可积累为资产**。产出物若不携带判定依据（4.1 的可判结构）且未经过判定（无判例地位），它对组织的资产贡献为零——只是仓库里的熵。这条要求把 4.1 的质量内建与 4.6 的资产结构接合成同一件事：**产出物必须以“判定证据”的形态存在，才是组织资产的正增长**。

**判定语言**：资产体系的质量由“**再生成成本**”度量——健康的资产体系，重新生成同等质量产出的完全成本（生成+验证）应显著低于首次积累的成本；再生成成本升高是资产退化的最早信号。

### 本章小结：产出治理总表

| 经典结论 | 依赖的人类参数 | agent 参数 | 新形态 |
|---|---|---|---|
| 质量内建优于末端检验 | 检验是瓶颈之一 | 检验是唯一瓶颈（1.3） | 可判结构内建为生存条件；检验灵敏度成为设计变量 |
| 官僚制目标置换风险 | 反馈环慢，退化以十年计 | 反馈环快，退化以月计 | 外部锚强制存在；架构级判定隔离；隐匿池轮换 |
| 判例法与案例推理 | 经验绑定判定者个人 | 执行者零经验积累 | 判例全物化（案例+理由+适用条件+快照）；一致性审查 |
| 委托模糊靠对话消解 | 对话协调成本可承受 | 对话不可扩展；生成趋零 | 独立多重执行+交叉比对=沉默区间自动定位 |
| 资产=因果模糊的资源 | 生成昂贵，产出即资产 | 生成趋零，产出可再生 | 资产=判定与积累体系；模仿瓶颈从理解变为时间 |

至此，产出治理的三块拼图完成：**判定怎么运行**（4.1-4.3 检验经济学、验证经济学、自指防护）、**判定怎么积累**（4.4-4.5 判例体系、规格暴露）、**积累物怎么构成价值**（4.6 资产形态）。组织侧（第三章）与产出侧（第四章）合起来，回答了 agent 组织的静态形态学。

下一章进入更深的构造层：第三章与第四章反复依赖的“规格”“判定标准”“角色权限”需要一个**统一的存在论声明**——组织如何显式定义“世界由什么构成、什么关系合法、什么操作被允许”。这是第五章（本体论工程）的工作：它不是一个工程实践话题，而是决策前提控制律在最深层的必然推论。

---

## 第五章 推导层 III：本体论工程——组织存在论的理论化

第三章与第四章完成之后，一个更深的构造问题浮出水面：两章反复依赖“规格”“判定标准”“角色”“权限”“证据”——这些概念在组织里如何获得**统一且可执行的声明**？当三个闭环各自理解“客户”一词、五个子系统各自定义“完成”状态时，一切治理语言在语义层开始漏电。本章论证：这个问题不是工程细节，而是决策前提控制律在最深层的必然推论——**组织必须对“世界由什么构成”做出显式、版本化、可执行的承诺**。这个承诺体系就是本体论，它在理论中的地位是宪法级。

### 5.1 本体论的理论地位：宪法级承诺的推导

> **组织的本体论是组织对“世界由什么类型的实体构成、什么关系连接它们、什么状态合法、什么操作被允许”的显式承诺。**

为什么一个组织理论必须包含本体论，且地位是宪法级？三条独立推导，每条从一条不变层结论出发：

**推导一（从决策前提控制律）**：控制行为的最高杠杆是控制决策前提（1.1）。一切决策前提的最底层是**本体承诺**——行动者对“存在什么”的假设决定它对“能做什么”的全部想象空间。一个 agent 若不被告知“承诺必须关联责任人”，它就不会在生成方案时为承诺分配责任人——不是因为能力不足，而是因为它的世界里没有这个实体关系。没有显式本体论的组织，agent 的本体承诺继承自其训练分布的隐式统计——**不可版本化、不可审计、不可治理的决策前提**。显式本体论是对最深一层决策前提的控制：比上下文白名单更深（白名单控制“看到什么内容”，本体论控制“世界有什么类别”）。

**推导二（从变异度律）**：治理变异度的表达载体是**约束谓词**——可执行的、能拒绝非法状态的判定逻辑。而谓词的语义基础是本体论：不知道“闭环必须挂接责任人”这个关系约束，就写不出“缺责任人则拒绝登记”的谓词。**本体论是治理语言的词汇表；没有词汇表的治理是劝喻，不是控制**。1.2 要求治理变异度匹配执行变异度——本体论的完备度直接决定可表达约束的上限。

**推导三（从验证上限律）**：跨组件的语义漂移——同一概念在多个子系统中的定义微妙地不同——是人审成本的结构性来源：每次人工对账（“甲系统的这个字段和乙系统的那个字段是同一个东西吗”）都是不可自动化验证的占比。本体论的解法是**单一声明、多处投影**：概念在唯一的源处定义，各子系统的本地表述由投影生成，一致性由生成器保证而非人工维护。这是定律二在语义层的中和手段。

**宪法级的三个含义**：

其一，**结构性修改归裁决层**：新增/删除实体类型、修改关系约束，等价于修改组织对世界的承诺——这改变一切 agent 的决策前提空间，属于 3.4 的裁决职能（S5 级），不可由执行闭环自治发起。

其二，**实例级修改是日常治理**：登记一个具体的实体（一个新的供应商、一份新的判定集）走正常流程——本体论的骨架与填充物分离，骨架宪法级、填充物流水级。

其三，**本体论版本参与校准有效性声明**——见 5.7，这是三条漂移触发线中最深的一条。

### 5.2 五层框架：Object / Link / Action / Function / Security

> **组织本体论由五层构成：Object（实体层）、Link（关系层）、Action（操作层）、Function（判定层）、Security（治理层）。五层缺一即残缺。**

每层独立论证其不可省略性：

**Object 层（实体层）**：世界由什么类型的实体构成。每个实体类型定义：稳定标识规则、属性集与类型约束、生命周期状态机。**为什么独立存在**：没有类型化的实体，一切数据是无 schema 的自由文本——而治理的第一公理是“无法对任意形态写约束谓词”。类型化是可治理性的最低门槛。

**Link 层（关系层）**：什么类型的关系连接什么类型的实体。每个关系类型定义：源/目标实体类型、基数约束、关系自身属性、**合法性谓词**（如“子闭环的决断时距不得超过父闭环”）。**为什么独立于实体层**：组织的核心约束多数定义在关系上而非实体上——“闭环必须有责任人”是关系约束，“承诺不得超出授权额度”是关系约束。把关系降级为实体的外键属性，就丢失了基数、级联、关系级安全标记的表达能力——治理词汇表直接残缺。

**Action 层（操作层）**：**类型化的、可审计的、带权限前置的写操作**。每个操作类型定义：参数 schema、前置条件、副作用声明（将修改哪些实体与关系）、权限谓词、必须产生的证据类型、失败语义。**为什么动词层是治理的实体（本章核心论证）**：实体清单可以复制、可以公开、可以由任何组织持有——但“在什么条件下允许做什么操作”的知识是治理的实体本身。一个只有名词没有动词的本体论是静态数据字典，不是组织本体论——**组织由“允许发生什么”定义，不由“存在什么”定义**。Merton 的官僚制研究早已发现同一事实的负面形态：组织实际运行由其规程（动词集合）刻画，正式结构（名词集合）从来只是名义。本体论的 Action 层是把这一洞见正面工程化。

**Function 层（判定层）**：绑定在实体类型上的可演化逻辑——评估定义、校准逻辑、漂移检测、度量计算。每个 Function 定义：类型化输入输出、所属校准域、版本。**为什么独立于 Action**：Action 是写路径（改变世界状态），Function 是读/判路径（对世界状态做出断言）。第四章的判定/执行分离原则在本体构造上直接体现——**执行者与判定者是不同类型的公民，它们的操作进入不同的层**。把判定逻辑混入操作实现（每个 Action 自带验证代码）使判定不可独立演化、不可独立审计、不可交叉比对——第四章的三重度量体系将失去结构支点。

**Security 层（治理层）**：贯穿四层的信任标记与权限谓词。每个实体、关系、操作、判定都携带安全标记（信任等级、归属边界、保密级别），路由与调用决策由 Security 层统一裁决。**为什么是独立层而非散落各处的注解**：安全策略需要独立演进（威胁模型变化时，结构不变而谓词全体更新——1.2 推论二的漂移场景）；散落的标记不可审计——“这个操作曾被允许吗”需要一个统一的裁决点回答。Security 层是变异度律在权限维的集中兑现。

**五层框架与既有实践的同构**：成熟的运营型本体系统（商业领域的 Foundry 式平台）收敛于完全相同的五层——这不是巧合，是同一组约束（治理写回、判定分离、可审计）下的必然解。本理论的贡献不在发明这个框架，而在**从组织公理推出它的必然性**，并把它从“某类平台的设计”提升为“任何 agent 组织的形态学要件”。

### 5.3 与六元组的映射：两种投影的同一结构

五层框架不是六元组之外的另一套体系——**它们是同一结构的分析与构造两种投影**：
六元组分量 本体论五层的对应
────────────────────────────────────────────
Intent（意图） → Object: 闭环类型 + 其挂接到责任人的关系
Context（上下文） → Object: 上下文资产 + 校准域实体
Capability（能力） → Object: 能力实体（执行器/工具）
Action: 允许的操作集合
Action（行动） → Action 层全体（类型化写操作）
Evidence（证据） → Object: 证据实体（回执/审计/校准事件）
Function: 判定逻辑（证据的判读）
Accountability（责任）→ Link: 责任挂接关系（必填约束）
Security: 裁决权限谓词

**映射的意义**：六元组是**分析语言**（审视任何闭环时看六个分量），五层是**构造语言**（搭建系统时组装五类构件）。一个闭环的每个分量在实现层都对应本体构件——**六元组回答“分析时看什么”，本体论回答“构造时建什么”**。两套语言的互译性保证分析与构造不脱节：审计一个运行中的组织（分析）发现的缺失，可以精确定位为本体构件的缺失（构造）——可判定性在语言层完成闭环。

### 5.4 嵌套与角色：继承结构的正确形态

组织是闭环的递归嵌套图（2.1），本体论必须表达这个递归。存在两条构造路线，其中一条是错的：

**类继承路线**（面向对象的直觉）：定义基类“闭环”，子类（客服闭环、开发闭环、资源调度闭环……）继承之。**为什么错误**：它把**嵌套**压扁成了**分类**——客服闭环内部的子闭环（分诊、处理、验收）与它的关系是“内部又是完整结构”（递归），不是“是它的特例”（分类）。类继承无法表达“闭环套闭环”——每个子类都是叶子，递归性丢失。这是把组织结构强行塞进类型系统的标准错误。

**嵌套+角色槽路线**（正确形态）：两层结构——

- **闭环实体表**：每个闭环是一个实体，父引用字段建立嵌套关系（递归无损）；
- **角色槽表**：实体在闭环中扮演的角色（执行、协调、控制、智能、身份、意图持有、上下文提供……）是独立的类型化槽位，槽位声明其负载类型约束。

**三个理论收益**：

其一，VSM 递归性获得数据层无损实现——每个闭环节点内部可挂完整五职能的子节点，任意深度；

其二，“角色稳定、执行者可替换”（2.1）获得结构表达——角色槽是稳定的 schema，槽的填充物（具体的人、agent、模型组合）是可替换的运行值。换执行者不动结构，这是 3.4 岗位重定义的构造基础；

其三，嵌套约束可直接写成关系谓词——2.3 的“子闭环自治不得放大父闭环约束”在这个结构上是 Link 层一条标准的合法性谓词，变异度律的组织约束由此获得本体层的可执行形态。

### 5.5 环境中立性

> **本体论框架本身必须环境无关：语言中立、存储中立、平台中立、工具中立。一切环境相关内容是实例化时注入的参数。**

推导（三条）：

**可移植性**：本体论定义的约束谓词（“闭环缺责任人则拒绝”）必须能在任何技术栈上执行——若框架绑定某类存储或引擎，理论适用范围被实现细节锁死，违反 0.4 的普适边界。

**多环境实例化**：同一份本体框架在不同环境（合规优先、成本优先、隔离网络）下实例化为不同参数——0.3 的“理论是分布、实例是采样”在本体层的直接体现。框架定义“实体类型必须有状态机”，不定义“状态机用哪个引擎执行”。

**本体论的规格化**：环境中立的本体 schema 本身可以写成规格声明，从而被多实例生成与多实例判定（第四章的独立多重执行可直接用于本体 schema 的质量检验——分歧处即 schema 的沉默区间）。**本体论成为它所描述的系统的一个规格对象**——这是自举结构的组成部分（2.3）。

### 5.6 本体论作为治理资产

三个资产属性的具体含义：

**其一，宪法上下文**。每个 agent 上线的初始决策前提必须包含本体契约的当前版本——agent 需要知道“这个世界有什么类别、什么关系合法”才能开始工作（推导一的工程化）。**agent 运行时上下文中必须记录其装载的本体版本号**——事后审计可以还原“当时 agent 以为的世界是什么样”。这条要求把本体论从文档变成了证据链的一环：本体版本 + 上下文装配记录 + 行为审计，三者合起来才能解释任何一次历史行为。

**其二，版本纪律**。结构性修改走裁决层签发流程，版本号独立递增，禁止静默修改——与理论文档自身的版本纪律同构（母版前言）。本体论的每次修改都是全组织决策前提的变更，静默修改等于在所有 agent 的世界观下方抽换地板。

**其三，双投影结构**。本体契约有两个人类可读/机器可读的投影：叙事文档（给责任层读——推导与理由）与 schema（给系统执行——结构与谓词）。**两投影必须同版本演化**——schema 变更而文档不变，裁决层在审议自己看不懂的世界；文档变更而 schema 不变，理论与实践开始分岔。分岔即 0.3 断裂判据的触发。

### 5.7 本体论版本与漂移：第三条触发线

> **对象语义是分布的最底层参数。本体论变更 ⇒ 实体语义变更 ⇒ 已校准判定的有效性存疑 ⇒ 触发重估。**

完整推导链：校准有效性声明覆盖“判定有效时，被判定系统处于什么分布”（2.2 扩展二）。被判定系统由本体实体构成——**实体语义是分布的最底层参数**。若本体变更（“承诺”的关系约束收紧了、“完成”状态机增加了新状态），则历史判定所描述的实体与当前 schema 描述的实体**不再是同类**——语义漂移已在 schema 层发生，判定器的输入分布随之改变，其校准在新语义下是否仍有效不能想当然。

由此，漂移治理存在三条正交触发线：

| 触发线 | 漂移源 | 检测对象 |
|---|---|---|
| **能力漂移** | 执行所依赖的外部能力组合变化（2.2 扩展二） | 行为分布 |
| **规格漂移** | 意图声明变化（第四章波次合并） | 判定对象 |
| **本体漂移** | 实体语义变化（本章） | 判定语言本身 |

第三条最深也最隐蔽：前两条漂移时，度量体系本身还能正常工作（它测量的是世界，世界变了）；**第三条漂移时，度量体系自己的语义地基在动**——校准域的声明字段必须包含本体版本引用，本体变更事件自动触发受影响闭环的重估工作流。不设这条线的组织，会在本体数次演化后发现自己的一切历史校准读数处于无定义状态——判定退化而无一声警报（4.3 自指问题的本体版本）。

### 5.8 单向信息梯度

> **信息只从高权限区向低权限区投影；反向读取与反向写入在架构上禁止。每个信息存储的位置必须声明其在梯度上的坐标。**

为什么这是本体论级结构而非安全实践？因为**梯度定义了实体的可见性语义**——同一实体类型在梯度不同位置有不同的投影形态（原件携带完整属性与来源指纹；投影携带裁剪后的子集）。**投影关系本身是本体 Link 类型**：原件-投影关系、脱敏管道是关系的物化实现。可见性不是部署细节，是实体的结构属性。

梯度的完整结构：
真值区（完整实体 + 判定基准的隐匿部分）
│ 单向投影（裁剪与脱敏）
▼
工作区（任务化投影——执行者所需的最小决策前提）
│ 单向镜像（密文形态）
▼
冗余区（灾备镜像——只读、加密）

三条不变量：**禁止反向写**（投影没有对原件的回写路径——投影上的修改不进真值区，须走受治理的回流流程）；**禁止跨梯度读**（低权限区的行动者无法读高权限区内容——4.3 的判定隔离由此获得架构保证而非纪律约定：执行侧的上下文装配中物理不含隐匿判定集）；**凭证方向性**（高权限区的凭证只授予向低权限区的写入权限）。

**梯度与判定独立性**：4.3 要求判定与执行的信息隔离“在架构上完成”——梯度就是这个架构。执行者永远拟合不了判定基准，不是因为它自觉，是因为隔离在信息拓扑上成立。**决策前提控制律最终落到信息拓扑上**——本章是那条推论链的终点站。

### 5.9 可见性分层：判定/执行分离的本体投影

五层框架与判定/执行分离有一个深层对应：

> **Object/Link 是共享层（判定与执行都需要）；Action 面向执行侧；Function/Security 面向判定侧。**

推导：执行（调用 Action）需要知道实体结构与关系存在（否则不知操作什么）；判定（运行 Function）需要知道实体结构与操作历史（否则无证据可判）。但——**执行者不需要知道 Security 谓词的全部内容**（只需知道被拒绝时的降级行为），**判定者不得持有 Action 的执行权限**（判定者可修改世界状态 = 判定可被污染 = 4.4 判例体系的一致性失效）。

工程含义：本体 schema 的不同部分对不同行动者有不同可见性——执行 agent 装配到 Object/Action 的投影（工具形态的 schema），判定 agent 装配到 Object/Function/Security 的完整访问。**可见性本身由 Security 层谓词裁决——本体论治理它自己的可见性**。这是自指的健康形态：系统的治理结构对自身适用且一致。

### 5.10 理论/实例分层的本体判据

本章收束于 0.3 的操作化——本体论层的分层判据：

> **判据：任何“只有特定组织才会如此”的本体内容，属于实例层。理论只承诺框架与必然形态；实例承诺具体类型清单与参数。**

| 内容 | 层次 | 理由 |
|---|---|---|
| “实体类型必须有稳定标识与状态机” | 理论 | 任何可治理组织的必然形态 |
| “操作必须绑定权限谓词与证据类型” | 理论 | 责任守恒律+六元组证据分量的构造化 |
| “存在校准域实体，其声明包含能力组合/规格版本/本体版本” | 理论 | 三条漂移线的构造化 |
| 具体有哪些实体类型 | 实例 | 特定组织的世界采样 |
| 信任等级分为几级、各级参数 | 实例 | 参数化选择 |
| 信息梯度分几区、各区在什么平台 | 实例 | 特定组织的信息架构采样 |

这张表是双向防线的操作化：**理论混入实例内容 = 普适性污染**（右列内容出现在理论中即违反 0.3 普适判据）；**实例无法回链理论条目 = 未理论化的偏好**（实例文档中每个本体决策应标注其理论依据）。本体论是这个防线最锋利的测试场——因为它同时是两层最具体的形式：理论的本体论定义“什么必须存在”，实例的本体论定义“什么恰好存在”。

---

## 附：文档族位置与版本记录

**文档族位置**：本理论有双投影与一组实例文档——本篇（人类版：论证与推导）、DGA-A（agent 投影：规范与判定条款，与本文同版本纪律演化）；实例层文档（策略、迁移路径、调研任务）由具体组织对本理论采样生成，不属于本理论。理论与实例的关系遵循 0.3：分布与采样。

**版本记录**：

- **v1.0**：首发。第〇章（总纲）；第一、二章（不变层：公理与定律、原语与度量）；第三至五章（推导层：组织形态学、产出判定、本体论工程）。
- **v1.0.1**：引用一致性修正，无语义变更，共五处——其一，§0.3 推导层范围声明自“第三至七章”修正为“第三至五章”；其二，§2.2 扩展一中对扫描资产化的前向章引用改为实例层承载；其三，§2.3 自举问题工程解的指向自原第七章改为实例层文档；其四，§3.2 对第四章的前向预告与第四章实际内容对齐；其五，第三章小结对第四章的预告同步对齐。其中第 1、3 项登记于断裂登记簿（见实例文档 DGA-MIGRATION 尾章，第 1、2 号条目）；第 2、4、5 项为文档定稿整理时发现的同类引用缺陷，一并补正（补登为断裂登记簿同类条目）。
- **v1.1**：实例吸收升版（理论-实例双流动中首次实例→理论方向）。其一，§4.3 新增结构性要求「其四」（判定体系自身的活性自检）——吸收自实例组织宪法 §4B 的 seed-drill 机制（每周向随机 PR 注入已知缺陷，关卡必须红），一般化为理论条款；其二，§4.3 新增「其二·附」（隔离的两种实现形态与威胁模型分层）——吸收自实例组织的考卷公开/答案密封架构裁决；其三，§1.7 新增预测观测登记块。无既有条款语义变更。裁决依据见实例文档 DGA-ADJUDICATION §11；本升版同时是断裂登记簿「理论升版」分支的首次运行记录。

