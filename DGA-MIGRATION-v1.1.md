# DGA-MIGRATION
## 建设与迁移路径 · 从现状快照到目标形态
### 版本 1.1

---

**文档性质**：本文档是实例层第二份文档。输入有二：DGA-STRATEGY 第〇章的现状快照（起点），DGA-STRATEGY 全文定义的目标形态（终点）。本文档的全部内容是两者之间的**路径设计**——差距分析、波次编排、自举核的构建与收缩、度量自动化、风险与回滚。

**回链纪律**：与 DGA-STRATEGY 同规则——每个实质性决策标注理论或策略依据（格式：DGA §x.x / STRAT §x.x）。路径设计中的参数选择（时限、门槛数值、优先序）是**实例参数**，标注为【实例参数】；它们可调，但调整须记录版本理由。

**术语**：本文档的“波次”沿用业务交付波次的机制（STRAT §5.3），但对象不同——迁移波次的产出是**组织基建**，交付波次的产出是**业务工件**。同一门禁机制，两类治理对象。混用时以上下文区分。

**单位声明**：迁移以波次为单位推进，**不承诺日历日期**。理由见总则第二条。

**执行状态声明（v1.1，裁决 FR-04，ADJUDICATION §5）**：本文档自 v1.1 起降级为**方法论备忘**——其 W0-W6 波次编排不作独立执行计划，门禁思想（对抗性测试定义验收）与自举核方法论（核成员判据/收缩协议/膨胀防线）并入既有 IR 流作为设计参照；迁移类工作此后一律走正常 IR。理由：线上组织（Cloudbird-Software）在本文档撰写期间已按自身宪法演化出与本文档目标形态高度同构的治理体系（实测重估见 §1.1 替换表），W 编排的大部分已被现实超越。防双 SSOT（原 ADR-0103 决策 6 原则适用）。第三章至第七章作为方法论参照全文有效（自举核协议、逐级晋升证据结构、度量自动化阶梯、风险回滚设计、迁移治理节律——它们描述的是方法，不绑定于已被超越的具体编排）。

---

## 第〇章 迁移总则

路径设计开始之前，五条总则约束一切具体编排。

**总则一：迁移自身受治理**（回链 DGA §0.1）。

迁移不是理论的应用豁免区，恰恰是理论的第一个大规模应用现场。每个迁移波次本身按六元组装配：意图（本波次目标）、上下文（差距分析与规格）、能力（自举核工具与 agent 劳动）、行动（建设）、证据（门禁测试结果）、责任（S5——创始人，具名）。**迁移波次在控制面建成后追溯登记为正式闭环**（第七章）。

**总则二：证据门禁制，非日历承诺**（回链 DGA §0.4）。

一人责任层加有限验证预算的参数下（STRAT 第〇章），日历估算是一种结构性虚构——它的失效模式不是“慢了”，而是“为了赶上日期降低门禁”，后者是理论红线（跳级即事故，DGA §2.3）。因此：**波次的进入与退出由证据定义，不由日期定义**。本理论以可判定性为终点，路径文档若以日期为单位即自我背叛。

**总则三：先决闭环拓扑序**（回链 DGA §2.3 先决条件结构）。

建设顺序不是偏好选择，是依赖图的拓扑排序：一切 agent 劳动消费外部能力 ⇒ **资源池治理先行**；一切判定资产需要存储与投影 ⇒ **信息架构第二**；一切业务治理需要规格与评估基建 ⇒ **工件体系第三**；业务闭环被治理后才谈得上扫描它 ⇒ **治理深化最后**。第二章的波次表是这个排序的展开，任何重排须声明依赖断裂理由。

**总则四：迁移遵守自己的验证经济学**（回链 DGA §1.3、§4.2）。

迁移产物的验证同样遵循冷启动组合：管道与基建的成熟模式优先参照公开实践（版本管理、持续集成、灾备演练的公开最佳实践即“已被大规模检验过的做法”，验证负担外部化，DGA §4.2 来源一）；S5 的注意力是全组织最稀缺资源 ⇒ **每个门禁的设计目标是最小化 S5 的验收动作时长**——判定由证据承载，S5 只裁决例外。

**总则五：核先行的螺旋扩张**（回链 DGA §2.3 自举问题）。

鸡生蛋问题的工程答案是：**先手工维持一个最小、显式声明、公示规模的核；波次逐一完成后，核内组件被规格化并移出；核单调收缩，治理螺旋扩张**。核的构建与收缩协议在第三章——它是本路径文档中最接近“理论实验”的部分：收缩曲线是 DGA §4.6 资产主张（价值在分布不在样本）在组织自身基建上的直接读数。

---

## 第一章 差距分析

### 1.1 缺口清单——实测重估表（v1.1，2026-09-06）

v1.0 规划缺口表基于"存量≈0"假设，已被 API 实测推翻；本表按实测重估（证据：DGA-02 差异分析 §1，GitHub API 实测+正本逐件阅读）。等级定义沿用：**0**=不存在；**1**=有雏形、未治理；**2**=已治理、未达标；**3**=达标。

| # | 目标结构 | v1.0 判 | 实测 | 实测证据（摘） | 差异说明 |
|---|---|---|---|---|---|
| 1 | 资源池 | 1 | **2.5** | providers.yaml 五条目零密钥目录；cost-check 硬停实弹验证；可删除层+漂移对账 | 缺 E1/E2/E3 信任级标签与路由谓词（FR-08 已裁决立即建） |
| 2 | 数据分级标签 | 0 | **1（不同轴）** | 证据账本三层（用途轴）+tenant 字段+INV-04 | 敏感度轴未建；全公开使一切=D-公开（FR-01 已裁决三阵地结构） |
| 3 | 真值区 | 1 | **2（拓扑冲突→已裁决）** | 声明面/记忆层/试卷层建制完备但全公开 | FR-01 裁决：CNB 私有阵地为真值区（考卷公开答案密封） |
| 4 | 物化管道 | 0 | **不适用→已裁决收缩** | 全公开下无真值→分发二分对象 | 收缩为密封引用（答案层），IR-0010 卡 B2 |
| 5 | 分发区 | 0 | **3（许可证除外）** | 20 公开仓、issue 回流通道、API 管道在用 | 许可证 FR-02 已裁决：治理仓补 MIT |
| 6 | 灾备镜像 | 0 | **0→执行中（唯一硬缺口）** | archive README 自报占位；无第二平台 | FR-03 已裁决：CNB+Gitee，IR-0010 卡 B3-B7 |
| 7 | 规格框架 | 1 | **2.5** | IR→spec→卡追溯闭合；契约兼容门 CI-5；spec-dsl/entropy 门 | 环境中立轴未建=非缺口（与宪法 §13"现在不建客户交付管线"一致） |
| 8 | eval 三层 | 0 | **2.5** | holdout 封存+诱饵+unseal 台账；eval-quad pin；verifier-exam 外部锚 | 缺轮换律自动化（补缺件） |
| 9 | 三重度量采集 | 0 | **2** | 语义熵分歧聚簇；eval-gate 非劣性；holdout_gap 护栏位 pending | 时代性缺（补缺件 4）；holdout_gap 数据源未建 |
| 10 | 判例库、负结果库 | 0 | **判例 2 / 负结果 1** | ADR 墓碑索引+32 卡语料+promotions 账本+误放行台账 | 负结果缺五元组 schema（补缺件 2，IR-0009 小卡） |
| 11 | 对齐物版本链 | 0 | **2** | eval-quad 基线 pin；CIW 钉版+canary→批量→回滚 | 缺显式版本链存档形态（登记规范缺） |
| 12 | 业务闭环治理化 | 1 | **2.5** | 9 产品仓四道门禁+conductor 状态机+AGENTS.md 协议块 | 六元组形式核查 negtest 待做（W4 门禁 1 思想移植，IR-0009 小卡候选） |
| 13 | 判定闭环（S3） | 0 | **3（超预期）** | arbiter 无 LLM/默认拒绝/CAS/防重放；eval-gate 零 LLM；seed-drill | 现状严于 DGA 目标——反向断裂，已被 DGA v1.1 吸收（判定活性自检条款） |
| 14 | 扫描闭环（S4） | 1 | **2** | patrol 三源场景生成；env-drift/cnb-audit/canary-sweep/CodeQL | 生态/方法/能力前沿未资产化（补缺件 3） |
| 15 | 研究闭环、前沿资产 | 1 | **1.5** | IR 流+patrol 前沿探索+负结果散记 | 前沿快照 v1 缺（补缺件 3，IR-0009 小卡） |
| 16 | 八度量仪表 | 0-1 | **2.5** | metrics.yaml 北极星对+注意力会计+元治理四列；pending 不造数 | 进度超前于本文档 §5.1 排序（注意力会计已覆盖验证比核心项） |
| 17 | 断裂登记簿 | 0 | **2（语义不同）** | ADR 墓碑+GM 门禁元治理+promotions 账本 | DGA 收编（IR-0009）后开通"理论-实例断裂"语义，升级为 3 |
| 18 | DGA-A 生产装载 | 1 | **1.5** | AGENTS.md 协议块 v2 全仓下发+drift §17 逐字节对账+四角色指引 | FR-05 已裁决：增量并线（四条义务入 ROLE-*.md） |

**重估汇总**：v1.0 判 0 的 9 行中，实测 0 的仅剩 1 行（#6 灾备）；判 1 的 5 行实测均值 2.1。差距主体已从"判定侧缺失"转移为"文档族与现实关系未定义"+四件补缺——迁移策略随之反转（见 §1.3 修订）。

### 1.2 依赖图与关键路径

缺口不是并列的清单，是依赖网。最长依赖链决定关键路径：
资源池(1,2) → 一切 agent 劳动
真值区(3) → 控制面日志 → 度量自动化(16) 中依赖日志的三项
真值区(3) → eval 三层(8) → 判定闭环(13) → 业务闭环达标(12) → 扫描治理(14) → 前沿资产(15)
物化管道(4) → 分发区(5) → 公开层度量与狗粮公示
工件体系(7,8,9,11) → 业务治理化(12)
业务闭环(12) → 研究闭环参数化(15) → 方向过滤器

**关键路径**：资源池 → 真值区 → 工件体系 → 业务治理化 → 治理深化。第二章波次序与此严格一致（总则三的来源）。

### 1.3 差距分析的三个结论（v1.1 修订）

其一，v1.0 结论一（缺口主体是判定侧）被实测反转：判定侧（#13 判定闭环 3 级超预期、#16 度量 2.5 级）恰是现状严于目标的部分；缺口主体=文档族与现实关系未定义（FR-06 元断裂）+四件补缺（灾备/负结果库/前沿快照/时代性）。v1.0 据此推导的紧迫性论证（"产出曲线倒计时已经开始"）由正向读数取代：治理先行下产出曲线持续上升（预测 1 反向分支）。本反转本身是方法论资产：**规划快照必须以实测校准，本文档自身即为该教训的登记载体**。

其二，v1.0 结论二（等级 1 项目是原料而非障碍）获实测印证：非正式端点使用已治理化为 2.5 级资源池，零散探索正参数化为研究闭环——物化显性知识的路径正确。

其三，v1.0 结论三（唯一不需要迁移的是责任层）获实测印证+正向读数：S5=1 人五周半不变，执行层扩张至 9 产品仓（预测 3 的 n=1 读数）。


## 第二章 建设波次编排

### 2.1 波次总表

| 波次 | 对象 | 前置 | 出口门禁 | 核收缩动作 |
|---|---|---|---|---|
| **W0** | 自举核构建 | —— | 核清单公示 | （核诞生） |
| **W1** | 资源池治理化 | W0 | 红线/熔断/漂移三项对抗测试 | 调度器移出（首次收缩） |
| **W2** | 信息架构 | W1 | 单向性/脱敏/灾备/滞后律四项 | 物化管道移出 |
| **W3** | 工件体系基建 | W2 | 三重度量可算/隐匿性审计/轮换首演 | 控制面启动器移出 |
| **W4** | 业务闭环治理化 | W3 | 六元组核查/证据链/度量基线 | 生成工具链移出 |
| **W5** | 治理深化 | W4 | 四扫描产出/研究闭环周期/前沿快照 | （核进入终态收缩） |
| **W6+** | 产品阶梯形成 | W4 基线稳定 N 波次【实例参数：N≥3】 | 另立波次族 | —— |

**串行与并行规则**：门禁串行（S5 注意力不可并行——DGA §1.3 验证瓶颈的组织形态），建设工作可并行——并行许可条件：依赖图无共享前置 + S5 批准。默认串行，并行是显式例外。

**状态注记（v1.1，FR-04）**：本表不作独立执行计划（头部执行状态声明）。对照读数：W1-W5 的等价物已大部分由现状体系与 IR-0006 完成（W1 资源池→治理 2.5 级；W2 信息架构→FR-01 裁决重构中；W3 工件体系→eval 三层 2.5 级；W4 业务治理化→9 仓 2.5 级；W5 治理深化→扫描 2 级+研究闭环参数化中）。残余执行内容已全部并入 IR-0009（收编+三小卡）与 IR-0010（私有阵地+灾备）。对抗性门禁设计原则（§2.2 开头段）继续作为一切 IR 验收设计的强制参照。

### 2.2 各波次详情与对抗性门禁

门禁的定义方式贯彻一个原则：**验收用对抗性测试定义，不用活动清单定义**——“做了 X”不是门禁，“试图让系统失败而系统正确拒绝并留下证据”才是门禁。这是 DGA §4.2 不可见基准思想的基建版：门禁本身是对系统的小型红队演练。

**W0 · 自举核**（第三章详述）。出口门禁：核成员清单公示于分发区 + 每成员的移出条件声明。W0 没有验证它的门禁——这正是核的定义（回链 DGA §2.3：第一个评估闭环之前无物可验证）。核的合法性由时限约束：**W1 门禁通过前，核的一切产出标记为“未治理状态”**，不进入任何对外交付。

**W1 · 资源池治理化**（STRAT §3 全章的落地）：

建设：端点登记表（E1/E2/E3 分级与依据）、数据分级标签体系、路由矩阵与红线谓词的实现、熔断与降级链、池变更→漂移事件工作流。

门禁三项：

1. **红线对抗**：构造违规路由请求（D-客户敏感数据 → 通用外部端点），系统拒绝并生成证据记录。通过标准：拒绝发生在路由层（不是事后审计层），证据含请求指纹与拒绝理由；
2. **熔断演练**：人为耗尽某端点预算，观察任务沿降级链有序移动；E1 判定任务降级时**只延迟不降质**（STRAT §3.4 规则的可观察验证）；
3. **漂移演练**：模拟池变更事件（登记一个新端点、变更一个现有端点属性），重估工作流自动触发，受影响闭环清单生成，相关判定输出携带“校准域外”标记。

**W2 · 信息架构**（STRAT §4 的落地）：

建设：真值区立仓（私有仓 + 控制面 DB）、物化管道全量（脱敏规则引擎）、分发区公开仓与许可证矩阵执行、灾备镜像（≥2 平台、密文）、回流评审入口、双源滞后运行化。

门禁四项：①**单向性对抗**（从分发区构造对真值区的回写请求，被架构拒绝——非流程拒绝）；②**脱敏抽检**（抽样投影件，人工核验无敏感残留——过渡期人工抽检，作为真实反馈通道建成前的临时真值源，W3 外部锚建成后逐步替代）；③**灾备恢复演练**（从镜像实际恢复一次，完整性校验通过，密钥全程未出真值区）；④**滞后律验证**（完整运行一个波次周期后，分发区落后真值区恰好一波次——不多不少，多了是同步失效，少了是判定资产暴露）。

**W3 · 工件体系基建**（STRAT §5 的落地）：

建设：规格框架（环境中立声明结构、契约层/实现层分离）、eval 三层结构、多实例扇出与交叉比对流水、holdout 管理、判例库与负结果库 schema、对齐物选择与版本链机制。

门禁五项：

1. **三重度量可算**：选一个试点工件，分歧率、还原度、时代性三个读数全部可计算并归档；
2. **隐匿性审计**：审计执行侧 agent 的上下文装配记录——holdout 内容在装配日志中物理不出现（对抗形式：审计员尝试从执行侧记录反推 holdout 内容而不可得）；
3. **轮换律首演**：新用例分流进隐匿池、老用例转正进可见集，至少各发生一次并留痕；
4. **首批资产入库**：判例与负结果各若干条【实例参数：各 ≥5 条】，五元组完整；
5. **对齐物 v1**：试点工件的最高还原度实例被选定、版本化、存档。

**W4 · 业务闭环治理化**（STRAT §1.3 两条软件线的治理装配）：

建设：两条业务线各按六元组装配——意图登记、规格引用、角色槽填充、DGA-A 装载（缺口 #18 在此关闭：业务 agent 生产装载 DGA-A v1.0）、证据 schema、责任锚点登记；L0/L1 起步运行。

门禁三项：

1. **六元组核查生效证明**：对业务闭环故意发起一次不完整装配（缺证据 schema 或责任人悬空），agent 依 DGA-A §A-10 拒绝开工并生成报告——**用 agent 的拒绝权证明治理已生效**（治理存在的证据是它能拒绝，不是它能执行）；
2. **完整交付波次证据链**：一条业务线完成一个从意图登记到验收归档的完整波次，证据链连续无断点；
3. **度量基线可读**：验证比、首次通过率、闭环延迟三项对业务闭环给出基线读数（数值本身不设门槛——这是基线采集，不是达标判定）。

**W5 · 治理深化**（STRAT §7 的落地）：

建设：四类扫描的运行化（扫描 spec 与周期）、研究闭环参数化、负结果复活流程、前沿快照机制、业务方向过滤器。

门禁四项：①四扫描各自产出至少一次资产更新事件（产出物是资产写入，不是报告——DGA §2.2 扩展一）；②研究闭环完成至少一个完整周期，正或负结果入库（负结果入库同样计为成功——研究闭环的产出就是结论本身，STRAT §7.1）；③前沿快照 v1 发布；④过滤器产出候选清单并经 S5 终裁一次（S5 独占决策 #4 的首次运行）。

---

## 第三章 自举核：构建、收缩与膨胀防线

### 3.1 核成员判据

一个组件属于核，当且仅当三条同时满足：

1. **依赖倒置**：它是某个尚未建成的下游体系的先决前提——在下游体系建成前，无法被“尚不存在的东西”规格化；
2. **人工直管**：当前由 S5 直接维护，无 spec+eval 覆盖；
3. **显式声明**：在公示清单上登记，含移出条件与目标波次。

三条缺一即不是核成员——缺第 3 条的未治理组件是**野生债务**（应立即处置：入核或规格化，二选一，不允许第三状态长期存在）。

### 3.2 初始核清单（W0 产出）

| # | 核成员 | 职能 | 移出条件（目标波次） |
|---|---|---|---|
| K1 | 生成工具链最小版 | 端点调用、工件生成的手工配置 | W4 后被业务闭环的正式能力层替代 |
| K2 | 控制面启动器 | 登记与日志的最小实现 | W3 被正式控制面替代 |
| K3 | 物化管道最小实现 | 手工触发的脱敏同步 | W2 被全量管道替代 |
| K4 | 资源调度最小闭环 | 手工路由表+预算记账 | W1 被治理化资源池替代 |

【实例参数】核规模双指标：**组件数（4）与 S5 周维护分钟数（初始基线在 W0 门禁时实测记录）**。两个指标同时公示——组件数会随移出下降，维护分钟数才是真实成本读数。

### 3.3 收缩协议

移出的完整条件（四项同时满足）：①**spec 覆盖**（组件行为有成文规格）；②**eval 覆盖**（存在判定其行为的评估，含至少一个对抗用例）；③**稳定运行一个波次**（在 spec 约束下运行无回退）；④**移出演练**（按 spec 重新生成一次并通过 eval——证明分布已可复现，样本可抛弃，DGA §4.1）。

**移出顺序按依赖深度**：叶子组件先行（K1 最先可移，K4 最后——它治理一切下游）。

移出不是删除：核成员移出后成为正常治理资产，其历史版本并入对齐物版本链。

### 3.4 膨胀防线

核的唯一允许增长路径：**新波次暴露出新的先决前提**，S5 批准入核，同时声明移出条件与目标波次。未附批准记录的核增长 = 断裂事件，登记入簿（STRAT §8.3）。

为什么核膨胀是自举失败的主形态：核是人工直管的——**核每膨胀一分，S5 的验证预算被基建吞噬一分**（DGA §1.3 的自指版本：迁移本身成为验证瓶颈）。收缩曲线的单调性因此不是整洁偏好，是组织生存条件。公开的收缩曲线（波次 × 核规模）是狗粮仪表之一（STRAT §5.7）。

---

## 第四章 逐级晋升计划

自治等级只能逐级晋升、以证据为前提（DGA §2.3）。各闭环的晋升计划：

| 闭环 | 起点 | 本迁移期目标 | 晋升证据要求 | 裁决 |
|---|---|---|---|---|
| 资源调度 | 核（人工） | L2 | L1：一批路由决策复核记录，红线零违反、熔断按序；L2：L1 期异常率与升级率数据 | S5 |
| 物化管道 | 核（手工触发） | L2 | 波次合并后自动执行 + 脱敏抽检作为里程碑门禁 | S5 |
| 判定闭环 | L0（S5 直审） | L2 | **特殊依据：外部对齐度，而非内部通过率**（见下） | S5 |
| 业务闭环 | L0 | L1 | 批次复核证据 + 首次通过率基线 | S5 |
| 扫描闭环 | L1 | L3 | L2：周期运行的门禁记录；L3：漂移信号自动升级的演练记录 | S5 |
| 研究闭环 | —— | 出生即 L2/L3 | 设计内属性：短时距、低预算、二元产出、异常即终止（STRAT §7.1） | S5 批准参数 |

**判定闭环晋升的特殊依据**（本章最重要的一条）：判定闭环自身的晋升，依据是**它对外部锚的对齐记录**，不是它对内部评估的通过率。推导（回链 DGA §4.3）：判定闭环的内部通过率是自指读数——判定器把内部题册答得再好，也不能成为放行它自治的证据，否则整条校准链失去终点。**验证体系自身的自治，必须由它验证不了的东西来裁决**——这是真值锚依赖律在晋升结构上的投影，也是先决条件结构（治理栈是它所治理的一切的先决闭环）的自反应用。

---

## 第五章 八度量自动化路线

### 5.1 各度量的自动化阶梯

度量的自动化本身进阶梯：L0 人工采集 → L1 agent 采集 + S5 抽检 → L2 自动采集 + 异常升级。

| 度量 | 现状 | 前提基建 | 迁移期达到 | 说明 |
|---|---|---|---|---|
| 闭环延迟 | 无 | 控制面时间戳（W2） | L2 | 依赖日志，最早可自动化 |
| 自治时距 | 无 | 控制面日志（W2） | L2 | 同上 |
| 升级率 | 无 | 升级事件流（W2） | L2 | 同上 |
| 外部对齐度 | 无 | 外部锚运行（W3） | L1→L2 | 锚建成后月度自动跑 |
| 首次通过率 | 无 | 业务证据 schema（W4） | L1 | 验收记录结构化后采集 |
| 上下文充分度 | 无 | 返工归因申报（W4） | L1 | agent 申报+抽检（A-50 裁量登记的读数） |
| 爆炸半径 | 无 | 依赖图+演练（W4 后） | L1 | 季度演练驱动 |
| **验证比** | 手工零星 | 人审行为结构化（最难） | L0→L1 | **最后自动化**：它度量人（S5）的行为，人的行为最难结构化记录 |

### 5.2 排序原则

先自动化**其他闭环的判定所依赖的度量**（日志三项——它们是判定闭环的输入），后自动化组织级度量（验证比——它判定组织本身）。度量的自动化顺序本身服从依赖拓扑——递归的一致性。

### 5.3 迁移期临时度量及其退役

迁移期引入三个临时度量：**核规模**（波次×双指标）、**门禁通过率**（一次通过 vs 返工）、**S5 时间分配**（基建/裁决/业务三分类）。它们是脚手架——**退役条件随 W5 门禁写入**：迁移完成即退役，退役本身留证据（判例思想的度量版：度量也有生命周期，退役时存档）。

验证比在迁移期的特殊读法：迁移期验证比**注定上升**（基建未成、人审偏多）——它不是恶化信号，是预期的爬坡成本。判定迁移健康度的替换指标是**迁移验证比的二阶导数**：上升速率应随波次递减（每波次的门禁证据化程度高于前波次）。【实例参数】W4 门禁时验证比应出现拐点——业务闭环治理化后，人审开始被评估体系替代。拐点未现 = 理论预测 1 的本地报警（DGA §1.7）。

---

## 第六章 风险与回滚

### 6.1 风险登记表

| 类 | 风险 | 触发信号 | 结构性缓解 | 回滚动作 |
|---|---|---|---|---|
| 供给 | 核心端点中断/配额收缩 | 任务失败率骤升 | 多源池（W1）；迁移早期单源依赖须显式声明过期时间 | 降级链（延迟优先于降质） |
| 数据 | 红线违反 | 对抗测试失败/审计发现 | 每波次红线对抗 + 季度演练 | 停止、证据保全、S5 直报（STRAT §8.2） |
| 漂移 | **静默漂移**（校准体系未建成期，判定质量悄然劣化） | 无信号——这正是风险本身 | 过渡纪律：S5 月度手工校准审查，显式声明为核外人工验证，W3 外部锚建成后退役 | 重估受影响判定 |
| 自举 | 核膨胀 | 收缩曲线非单调 | 成员判据+批准记录+断裂登记 | 核冻结+债务审计 |
| 晋升 | 跳级 | 升级率异常/爆炸半径超限 | 逐级表+证据前置（第四章） | **授权收回** |
| 人力 | S5 时间被迁移吞噬 | 迁移验证比上升速率不衰减 | 门禁证据化设计 | 波次暂停（显式优先级裁决，非失败） |

**静默漂移行的特殊说明**：它是唯一没有触发信号的风险——在 W3 之前，组织对自身判定质量处于部分盲区。这个盲区不可消除（自举问题的必然代价），只能**显式承认并手工覆盖**：S5 月度校准审查是核之外的第二个人工孤岛，与核一样有退役条件。理论诚实地要求：过渡期的一切人工验证都必须声明退役路径，否则它们会成为永久性暗物质。

### 6.2 回滚设计

**状态向量**：组织在任何时刻的完整可回滚状态 = （规格版本集，对齐物版本链，核清单，控制面状态，授权级别集）。回滚 = 重指向先前的版本集——由资产版本化保证低成本（STRAT §5.3 版本链原则的迁移应用：我们为业务设计的机制，同时是我们自己的保险）。

**回滚的三个层级**：

1. **组件回滚**：某波次产物缺陷——规格版本回退，对齐物重指向旧版；
2. **闭环降级**：某闭环自治失控——授权收回（L2→L1），即时生效，无需重建（降级是治理动作，不是故障修复）；
3. **波次中止**：门禁失败且短期不可修复——波次显式暂停，S5 签发优先级裁决。

**回滚哲学**：回滚设计要对抗的主要敌人不是技术故障，是**沉没成本对回滚的抵抗**——“再跑一周看看”是人类组织的标准死法。结构性解法：降级动作在治理上定义为正常操作（与升级对称），其执行成本趋零（授权级别集是状态向量的一部分，改一个字段的成本），心理成本被结构成本覆盖。年度全量恢复演练（从灾备镜像重建状态向量）验证整条链。

---

## 第七章 迁移的治理

**门禁评审节律**：每波次出口一次 S5 评审，证据包三件：门禁对抗测试结果、度量增量、断裂登记摘要。【实例参数】单门禁 S5 直审时间目标 ≤ 60 分钟——超时即门禁证据化不足的信号，门禁本身需要返工（治理治理者的递归）。

**断裂协议的迁移期条款**（STRAT §8.3 的扩展）：任何门禁失败若暴露理论缺口，登记入簿；迁移期增加提前触发条款——**阻塞性断裂（阻止波次推进的理论缺口）48 小时内裁决**，非阻塞断裂维持月度审议。

**追溯装配**：W0-W2 发生时控制面可能尚不存在，这些波次在控制面建成后**追溯登记为闭环**。纪律：重建的证据记录必须标注“重建”——不将重建记录伪装为实时记录（DGA-A §A-50 未测声明的文档级对应：诚实声明证据的来源是证据链自身可信的前提）。

**狗粮公示**：迁移仪表（核收缩曲线、门禁通过率、迁移验证比拐点）随分发区公开——W2 分发区建成后生效，此前的公示随 W2 门禁补档（同样标注补档）。

---

## 尾章 撰写本文档时发现的理论缺陷登记

撰写本章路径设计时，发现理论母版（DGA v1.0）存在两处引用缺陷——迁移路径移入实例层的裁决执行后，理论内部的章节引用未同步更新：

1. **DGA §0.3** 文档分层声明“推导层（第三至七章）”——实际推导层为第三至五章（迁移路径与探索治理移出后的正确范围）；
2. **DGA §2.3** 自举问题段落引用“第七章（迁移路径）专门处理它的工程解”——该内容现已定位于实例层文档（本文档第三章）。

依版本纪律（禁止静默修改）执行补丁：**DGA v1.0 → v1.0.1**，两处引用修正，无语义变更。登记为断裂登记簿第 1、2 号条目，类别：文档引用缺陷（非推导链断裂）。此事件同时完成两件事：断裂登记簿（缺口 #17）以真实条目开张；理论与实例的版本演化联动（STRAT 前言承诺的“理论升版后一波次内完成回链校对”）的首次演示。

---

## 附：版本记录

- **v1.0**：首发。第〇章至第七章 + 尾章（含理论缺陷登记与 v1.0.1 补丁签发记录）。
- **v1.1**：执行状态降级与实测重估（应用补丁单 M-P-1~M-P-5）。其一，头部执行状态声明：W0-W6 编排不作独立执行计划，本文档降级为方法论备忘，第三章至第七章作为方法论参照全文有效（FR-04）；其二，§1.1 替换为实测重估表（18 行，证据：DGA-02 差异分析 §1，FR-07）；其三，§1.3 三个结论修订（结论一实测反转，结论二/三获印证）；其四，§2.1 状态注记（波次对照读数+残余执行并入 IR-0009/IR-0010）。无方法论内容变更；尾章理论缺陷登记（v1.0→v1.0.1 补丁记录）作为历史档案保留。裁决依据：DGA-ADJUDICATION §5/§8。

