# DGA-TOOLING
## 工具选型调研任务 · 结构不变量约束内的评估与下发
### 版本 1.0

---

**文档性质**：本文档是实例层第三份、文档族最后一份。输入有二：DGA-STRATEGY 锁定的**结构不变量**（信息梯度三不变量、数据红线、许可证矩阵、池分级），DGA-MIGRATION 编排的**波次拓扑**（哪些选型是哪个波次的先决输入）。本文档的产出不是选型决定——**本文档不作出任何选型决定**（选型决定权在 S5，STRAT §2.2 独占决策 6 之工具形态），产出物是**可下发的调研任务定义**：每个任务是一个扫描闭环的特化工作（STRAT §7.5），其产出是候选对比证据与处置建议，供 S5 裁决。

**回链纪律**：与前两份实例文档同规则。

**一个前置裁决**：本文档大量内容涉及具体平台与工具的候选名单。这是合法的——实例层文档本就承载具体组织的具体采样（DGA §5.10 分层判据）。但名单中的任何名字都不是推荐，只是**调研起点池**：候选的取舍由任务返回的证据决定，不由本文档的列举顺序暗示。

---

## 第〇章 选型的方法论前提

### 0.1 造还是买：信任-验证梯度的工具投影

工具选型的第一问题不是“哪个好”，是“这个槽位该不该买”。判定框架来自理论的组织边界推导（DGA §3.6）：

| 槽位 | 判定 | 依据 |
|---|---|---|
| 控制面（登记、日志、状态向量） | **造** | 治理核心闭环永远内化——它是定义性资产 |
| 规格框架、脱敏规则引擎、判定逻辑 | **造** | 同上：判定体系的逻辑本体 |
| 代码托管、对象存储、密文工具、数据库引擎 | **买** | 通用基建——成熟公开实践，验证负担外部化（DGA §4.2 来源一） |
| 模型能力 | **买** | 已由组织形态决定（STRAT 第〇章：一切执行外购） |
| eval 执行环境（沙箱、运行器底座） | **买件造壳** | 底座是通用件，判定编排是核心 |

**造的预算纪律**：自建项全部落在自举核或其直系后代（MIGRATION 第三章）——自建规模受核收缩曲线约束，防止“造”变成 S5 注意力的无底洞。

### 0.2 评估的两级结构：否决项与参数项

> **否决项（veto）：结构性不变量的直接投影——违反即出局，不参与任何权衡。参数项：只在否决通过者之间比较。**

这个两级结构是理论红线纪律（STRAT §8.2）在选型上的形态：不变量不是偏好，违反“数据不出信任域”的候选，无论参数多好都不进入比较——这与日常直觉（“它便宜三倍”）的冲突正是否决项存在的意义：**便宜三倍买到的是越权**（STRAT §3.2 原文措辞的选型版）。

### 0.3 选型自身的治理（递归条款）

选型过程本身按理论运行，三条：

其一，**调研任务是研究/扫描闭环**：六元组装配（意图=本任务目标、上下文=不变量与候选池、行动=调研与测试、证据=对比矩阵与原始记录、责任=S5），产出是证据不是决定——任务闭环的判定对象是“证据是否充分支持对比结论”，不是“选谁”。

其二，**试用期是微型波次**：任何选型落地前进入试用期（spec=选型参数声明，eval=验收标准，退出条件=证据定义，第四章展开）。

其三，**供应商锁定即爆炸半径**：每项选型登记其锁定深度与退出成本，纳入依赖图——工具的版本升级、条款变更是供应链事件，触发重估（与模型端点的漂移治理同构）。

### 0.4 任务下发协议

每个任务按下述模板定义，任务即闭环，agent 按 DGA-A v1.0 装载执行：
任务 = {
编号 T-xx，对象，目标波次（对齐 MIGRATION 拓扑），
意图：回答什么问题，
否决项清单（结构性，不可协商），
参数项清单（比较维度与权重建议），
方法：调研/测试/审计的具体设计，
产出 schema：登记簿条目 + 处置建议，
预算与时距上限【实例参数】，
禁止事项：任务不得做出的决定（一切“选定”动词归 S5）
}


---

## 第一章 信息架构平台调研任务族

（对齐 W2；候选池为起点，非推荐）

### T-01 真值区平台调研

**意图**：私有仓与控制面宿主的选型证据。

**候选起点池**【调研确认】：自部署方案（GitLab CE、Gitea/Forgejo 类轻量方案）；云托管私有仓方案（主流平台的私有仓库形态）。

**否决项**：①内容控制——云方案的条款不得索取训练权、分析权或内容使用权（D-客户敏感与 holdout 的存储前提，STRAT §3.2）；②凭证方向性可配置（写入与读取权限可分离表达，DGA §5.8 第三不变量）；③API 完备——物化管道与控制面的自动化前提；④审计日志可导出（证据链的存储前提）。

**参数项**：运维负担（**自部署方案的此项特殊：运维分钟数直接消耗 S5 时间——计入核维护成本**）、迁移成本、与灾备镜像的集成度、密文存储友好度。

**提交 S5 的裁决框架**（本任务的核心权衡，理论地位明确）：自部署 vs 云私有仓是 DGA §3.6 信任-验证梯度的教科书案例——自部署以运维成本换取控制让渡为零，云方案反之。任务产出两种方案的完整成本-风险对比，**不做倾向性建议**；这是 S5 的边界裁决（独占决策 6），不是参数比较。

**产出 schema**：对比矩阵（否决项通过记录逐项列明、参数评分附原始证据链接）+ 双方案成本模型 + 迁移成本估计。

### T-02 分发区平台调研

**意图**：公开仓（开源实现、公开 eval、理论发布）的宿主选型证据。

**候选起点池**【调研确认】：面向开发者的主流公开托管平台（国际与国内分别列举，触达受众是参数而非否决项）。

**否决项**：①AGPL 合规无障碍（网络条款在平台上的执行不设障碍，STRAT §5.4）；②开放 issue 系统（**回流通道的存在前提**——公开用户的分布外探测，STRAT §4.2）；③API 写入支持物化管道的单向推送；④免费额度覆盖分发规模（分发区内容全部为公开层资产，付费无正当性）。

**参数项**：目标受众触达、CI 能力、速率限制、社区生态位。

**滞后律的接口要求**（结构性）：平台的写入 API 必须支持“每波次合并后批量推送”——滞后一个波次的同步节律（STRAT §4.2）要求平台侧有原子化批量操作能力。

### T-03 灾备镜像平台调研

**意图**：≥2 个独立平台的密文只读镜像选型证据。

**否决项**：①**平台独立性**——与真值区、分发区所选平台不共享故障域（不同运营主体、不同基础设施；独立性是灾备的全部意义，同公司多产品不算独立）；②密文存储无条款风险（密文形态下平台无法接触内容，条款审计聚焦“密文本身的存储与保留条款”）；③恢复验证机制可行（镜像的可达性与完整性可周期性自动校验）。

**参数项**：免费额度、耐久性声誉、地理分布、API 速率。

**特别条款**：密文使免费额度合格（STRAT §4.3——降级的明文可用性是设计好的，密钥在真值区）。**免费平台的密文镜像不构成资产降级**——本任务的参数比较因此与直觉相反：耐久性与独立性优先，平台档次其次。

### 第一章附：三区选型的耦合约束

三任务分别产出，但裁决必须联合进行（S5 一次裁决三个选型）：真值区-分发区之间是物化管道方向（T-01 的 API 与 T-02 的 API 必须兼容管道实现）；真值区-灾备之间是独立性约束（T-03 的否决项依赖 T-01 的结果）。**任务独立执行、裁决联合进行**——证据生产并行，决定生产串行（S5 注意力的串行性，MIGRATION 总则）。

---

## 第二章 模型端点池调研任务族

（对齐 W1——**本族是最高优先任务族**：资源池治理化是第一个先决闭环）

### T-06 端点条款与合规审计（先于 T-04/T-05 执行）

**依赖声明**：条款审计是能力测试的**先决任务**——测试数据将发送给被测端点，若条款未先审计通过，测试行为本身就构成数据暴露。顺序不可倒置，这是数据红线在任务编排上的投影。

**意图**：候选端点的条款风险画像。

**候选起点池**【调研确认】：国际商用 API、国产商用 API、开源模型自部署方案，三类分别列举。

**审计清单**：①训练权条款（输入是否被用于提供方训练——**D-内部数据路由的前提**，STRAT §3.2 红线第二条的条款侧）；②数据保留条款（输入/输出的保留期限与地域）；③合规属性（数据出境的合法性边界——与 D-客户敏感的“本地部署或合同锁定隔离端点”红线对照）；④配额与可用性条款（熔断设计的输入）；⑤断供风险（提供方对 API 形态的承诺历史与变更记录）。

**产出 schema**：端点条款画像表（逐条目引用条款原文，不转述——条款审计的证据是原文）+ 分类结论：**可路由数据级别**（D-公开 only / D-内部可 / 可承载客户敏感——最后一级原则上仅本地部署方案）。

**条款监控的持续化**：本任务不是一次性——审计条目转入生态扫描的持续监控对象（STRAT §7.5），条款变更即漂移事件（STRAT §3.3 同构：端点集合变更触发校准重估）。

### T-04 E1 判定级端点能力边界测试（前沿资产首批采集）

**意图**：判定级候选的能力边界地图——STRAT §7.3 能力前沿资产的第一个正式采集任务。

**方法设计**（本任务的方法论含量最高，四条）：

1. **测试集构造**：判定类任务的边界用例——holdout 判读样本、证据判读的对抗用例（诱导性证据、部分证据、伪证据）、漂移检测的难度梯度用例。构造来源按冷启动优先序（STRAT §5.2）：公开基准改造 > 合成生成（外部锚校验）> 人工标注（最后手段，S5 批准）；
2. **跨端点交叉**：同一边界用例集对多个候选执行——**不同端点对同一用例的表现差异，就是能力分布的独立采样**（DGA §4.5 独立多重执行对端点评估的应用）；
3. **失败模式分布映射**：不只记录通过率，记录**失败的形态**（哪类判定失败、失败时的置信度表现、失败的可检测性）——调节器定理（DGA §2.2）要求判定体系建模被判定者的行为分布，失败模式分布是分布建模的核心素材；
4. **评估独立性纪律**（本章最重要的结构约束）：**被评估端点不得判读自己的测试结果**。判定级端点的评估必须由外部锚（公开基准）+ 跨端点交叉承担——E1 的候选是“裁判的候选”，裁判资格不由自己的答题册证明（DGA §4.3 判定/执行分离在端点评估层的投影）。

**产出 schema**：端点边界测试记录（用例×端点的表现矩阵 + 失败模式分类）→ 直接入前沿资产（边界测试记录的首批条目，STRAT §7.3）+ E1 池组成建议（含冗余度：**每级别 ≥2 提供方**，爆炸半径控制）。

### T-05 E2/E3 端点调研

**意图**：执行级与批量级的池组成证据。

**方法**：E2——常规执行任务的代表集测试（两条业务线的典型任务样本，能力/成本二维读数）；E3——脱敏数据与粗筛任务的**成本效率测试**（单位产出成本是主导参数，能力下限设为否决项而非比较项：下限之上的更便宜者胜出）。

**E3 的数据纪律**：测试用脱敏数据或 D-公开数据——批量级端点的测试不得以真实敏感数据为成本（又一条款审计先行的理由）。

**产出 schema**：分级池组成建议（每级冗余 ≥2）+ 降级链参数建议（E1→E2→E3 的任务迁移在候选池上的具体路径，STRAT §3.4 的实例化输入）。

---

## 第三章 工具链与协议层调研任务族

（对齐 W2/W3；多数槽位已由 0.1 裁决为“造”，本族任务相应收缩）

### T-07 规格框架基座选型

**裁决**（0.1）：框架自建——规格框架是定义性资产（环境中立的本体 schema 声明结构，STRAT §5.1）。

**任务收缩为**：声明格式的基座选型——schema 语言与验证工具的成熟件对比（JSON Schema 及其生态、类型化声明语言等候选）。否决项：环境中立（基座本身不绑定运行时，DGA §5.5）；参数项：验证工具成熟度、agent 可读写性（双投影的机器侧）、人类可读性（叙事侧）。

**与 K1 的关系**：本选型是自举核 K1（生成工具链）的构件——选型结果直接进入核清单的移出条件声明（MIGRATION §3.2）。

### T-08 eval 执行环境选型

**裁决**：壳自建（判定编排是核心），底座外购（沙箱与运行环境）。

**否决项**：①沙箱隔离强度（执行 agent 的测试环境不得触达真值区——隔离失败的爆炸半径是红线级）；②可观测性（沙箱内行为可完整记录——证据 schema 的前提）。

**对抗性验收**：本选型的门禁包含**沙箱逃逸测试**（构造逃逸尝试，验证被阻止且留痕）——W1 门禁对抗思想（MIGRATION §2.2）在工具层的直接复用。

### T-09 控制面数据层选型

**裁决**：买（通用基建）。

**否决项**：①审计日志 append-only（登记记录不可篡改——证据链完整性的存储级保证）；②时间戳可信（闭环延迟与自治时距的度量前提）；③权限谓词可表达（Security 层的存储投影，DGA §5.2）。

**候选起点池**【调研确认】：主流关系型数据库（含其审计能力形态）。参数项：运维负担（真值区自部署的整体成本）、与物化管道的集成、备份加密支持。

### T-10 密文与脱敏工具选型

**裁决**：加密工具买（成熟公开实践——密码学工具的验证负担由公开生态外部化，DGA §4.2 来源一的经典情形）；脱敏规则引擎造（规则本体是业务核心）。

**否决项**：①密钥不出真值区（密钥管理方案的架构约束，STRAT §4.3）；②加密格式的长期可读性（灾备恢复的时间跨度要求——十年后仍可解密的格式纪律）。

**密钥管理的特殊裁决点**：密钥托管方案（KMS 类服务 vs 本地密钥管理）是信任-验证梯度的又一次 S5 边界裁决——任务产出对比，不做倾向建议。

---

## 第四章 试用与登记协议

### 4.1 试用期设计

每个选型（S5 裁决后）进入试用期，试用=微型波次：
试用期 = {
spec : 选型参数声明（要什么、给什么、红线是什么）
eval : 验收标准（可测量的试用期退出条件）
时长 : 证据定义，非日历——退出条件达成即申请转正
退出 : 双向——转正（入登记簿正式条目）或退回（组件回滚，MIGRATION §6.2 层级一）
}

**试用失败的预设处置**：退回不是失败事件，是选型闭环的正常产出（负结果入库——该工具在此用途上不可行，原因与条件登记，供应商参数变化时复活评估，STRAT §7.2）。**试用协议的存在本身是回滚哲学的实施**：低成本尝试、低成本退出（MIGRATION §6.2：对抗沉没成本的结构解法）。

### 4.2 工具选型登记簿

**条目 schema**：

登记条目 = {
工具标识、版本、用途槽位，
依赖类型 : 直接 / 对齐 / 借鉴（STRAT §5.4 三分法——对工具同样适用：
直接依赖升级须审计重估；对齐依赖是行为参照；借鉴是思想来源），
否决项通过记录（逐项、附证据链接、含审计日期），
参数评分（试用期内实测，非调研期报价），
锁定深度与退出成本（爆炸半径评估），
条款画像引用（T-06 条目的链接），
状态 : 试用中 / 转正 / 退回 / 退役
}

**登记簿与漂移的接合**：登记工具的版本升级、条款变更、供应状态变化是供应链事件——触发依赖它的闭环重估（与端点池的漂移治理共用一条事件流）。**登记簿因此不是静态文档，是依赖图的可查询投影**——爆炸半径度量的数据源（季度演练的输入，STRAT §8.1）。

### 4.3 决策流
任务下发（agent 扫描闭环）
→ 证据产出（对比矩阵 + 处置建议，入登记簿草稿区）
→ S5 裁决（消费摘要与建议；证据在登记簿待查——S5 时间预算约束【实例参数：
单项裁决 ≤ 30 分钟，超时即证据组织不足的信号】）
→ 试用期（微型波次）
→ 转正登记 / 退回入库

裁决流的结构保证：**S5 永远裁决“证据是否充分 + 边界如何划”，不裁决参数细节**——参数比较由任务的评分矩阵承载，S5 只在否决项解释有争议、边界权衡（自部署 vs 云、KMS vs 本地）时行使判断。这既是 S5 时间的保护（DGA §1.2 推论一），也是决策可追溯性的要求（裁决记录引用证据条目）。

---

## 第五章 任务总表

| 编号 | 对象 | 类型 | 先决依赖 | 供给波次 | 产出 |
|---|---|---|---|---|---|
| **T-06** | 端点条款审计 | 扫描+审计 | —— | **W1**（最高优先） | 端点条款画像、可路由级别 |
| **T-04** | E1 能力边界测试 | 研究闭环 | **T-06** | W1 | 边界测试记录→前沿资产首批、E1 池建议 |
| **T-05** | E2/E3 调研 | 研究 | T-06 | W1 | 分级池建议、降级链参数 |
| **T-01** | 真值区平台 | 调研 | —— | W2 | 对比矩阵、双方案成本模型 |
| **T-02** | 分发区平台 | 调研 | —— | W2 | 对比矩阵 |
| **T-03** | 灾备平台 | 调研 | T-01 结果（独立性约束） | W2 | 独立性验证的候选对比 |
| **T-07** | 规格基座 | 调研 | —— | W3（K1 构件） | 基座选型证据 |
| **T-08** | eval 执行环境 | 调研+对抗测试 | —— | W3 | 沙箱选型+逃逸测试记录 |
| **T-09** | 控制面数据层 | 调研 | T-01 | W2/W3 | 数据层选型证据 |
| **T-10** | 密文工具 | 调研 | —— | W2 | 加密选型+密钥管理裁决材料 |

**任务族的节律**：第一波下发 T-06（立即——它是 T-04/T-05 的先决且不依赖任何基建）；T-06 返回后并行下发 T-04/T-05 与 T-01/T-02；T-03、T-09 随 T-01 结果跟进。全部任务的负结果与中间发现入负结果库——调研过程本身就是研究闭环的运行（STRAT §7.1 的第一次实战）。

**任务执行的 agent 装备**：执行任务的一切 agent 生产装载 DGA-A v1.0（MIGRATION W4 的前移应用——任务闭环先于业务闭环装载，装载记录入控制面或过渡期手工台账【核外人工验证，声明退役路径：控制面建成后转正式登记】）。

---

## 尾章 文档族完成声明

**DGA-TOOLING v1.0 完，文档族五份齐备**：

| 文档 | 层 | 性质 | 版本 |
|---|---|---|---|
| DGA | 理论 · 人类版 | 公理、定律、原语、形态学推导 | v1.0.1 |
| DGA-A | 理论 · agent 投影 | 规范与判定条款 | v1.0 |
| DGA-STRATEGY | 实例 · 策略 | 理论在本组织参数下的采样 | v1.0 |
| DGA-MIGRATION | 实例 · 路径 | 现状到目标形态的波次编排 | v1.0 |
| DGA-TOOLING | 实例 · 任务 | 选型调研的可下发定义 | v1.0 |

**文档族自身作为第一个工件的六元组装配**：意图=本组文档的存在理由（理论的确立与本组织对其的采样）；上下文=理论体系+现状快照；能力=agent 劳动与 S5 裁决；行动=五份文档的撰写；证据=全文的回链标注、断裂登记簿已开张（含真实条目）、登记簿 schema 已定义；责任=S5（创始人，具名）。六元组齐备——**文档族是第一个按理论规则构建的工件，它自带判定依据**。

**组织状态的切换声明**：文档撰写阶段结束，闭环运行阶段开始。下一步动作序列已经完全确定且互相解锁：T-06 立即可下发（零前置）→ T-06 返回解锁 T-04/T-05 与 W1 建设 → W1 门禁解锁 W2 → 自举核开始收缩。此后的一切进展由证据门禁驱动，不由文档驱动——**理论的第一个可证伪预测（产出曲线先升后降，除非治理先行）从 T-06 下发的时刻起进入实测**。

狗粮仪表等待第一批读数。理论自此接受它自己规定的审判方式：可测量、可判定、可证伪。

---

## 附：版本记录

- **v1.0**：首发。第〇章至第五章 + 尾章（文档族完成声明与状态切换声明）。




